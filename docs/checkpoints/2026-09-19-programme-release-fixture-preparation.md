# Programme actual-owner first-release fixture preparation

Date: 2026-09-19. Scope: P07 command and typed/JSON/calendar composition inside
#108/#48. No native execution, profile promotion or production use.

## Prepared outcome

SCH-004/006/007/008/010/012, HR-014/015, PRG-008, AUD-001 and NFR-013 follow
ADRs 0081, 0093–0097 and 0099. Existing production commands, permissions, schema,
current profiles and runtime guards are unchanged. No new recipe or ADR is needed.

The guarded child validates the original seven-stage setup-through-staffing chain
and creates one eighteenth distinct verified synthetic reviewer. Original
controllers independently approve thirteen existing explicit recipe assignments.
Source access remains separate from review/publication, and Venue dependencies
use the actual two selected-room targets. This is not a catch-all role or an
Organization-wide Venue grant. The synthetic planner and reviewer deliberately
both receive review/publication capabilities so negative cases test retained
authorship and independent publication, rather than missing permission. This is
a test arrangement, not a recommended allocation of real responsibilities.

Both people freshly collect the complete exact candidate through actual owner
queries. All ten categories must be satisfied with one common digest and no
finding; unexpected warnings, missing sources or inapplicability stop preparation.
Nothing is automatically acknowledged. The author cannot approve, a distinct
person approves, the approver cannot publish, and a stale pointer refuses
publication without changing the prior absent release. Original approval and
publication retries retain identity. The checked active manifest must identify
the original three placements and independently reviewed renditions.

Actual independently authorized public, exact-person and operator queries feed
the real JSON/calendar serializers. Both hosts see only their own released
presences. The volunteer retains the three exact confirmed commitments and
original work times; no hosting purpose or unrelated release existence is
disclosed by that private layer. Edition, Department and each selected room use
their own output query, explicit staffing layer, exact membership and linked
locked demands. Delivery layers are not silently added. These are point-in-time
owner/format checks, not a native HTTP, browser, print-dialog or offline guarantee.

The original private pipe, fixed child module, runtime admission and finite lease
remain mandatory. Input/output bounds, same-source decoding and private error
handling are retained. The host-only native case adds real runtime read-only
observations for separate retained actors, candidate/digest, one artifact/pointer,
unchanged commitments and no Participation. It is uncollected/unexecuted #102 debt.

## Verification and continuation

Focused database-free tests passed 114 cases in 0.82s: real owner signatures,
typed preflight/receipt data, closed source decoding, guard/process bounds,
independence/stale/retry failure branches, exact audiences and actual pure output
serializers. Iteration caught a synthetic password/digest test collision and an
operator DTO field mismatch; corrected candidates passed. This is not native
authorization, concurrency or transaction evidence. Complete database-free
feedback passed 11,241 cases in 61.82s with three existing Django URL-field
warnings. Ruff formatting/lint, whitespace and documentation validation passed
(637 Markdown files, four repository skills, 215 requirements). Exact certification
is pending at this snapshot.

PR #184's protected evidence is appended to its prior checkpoint. Continue P08
successor/retained-work impact and independent notices, P09 on-site/rendered/print
journeys, then P10–P12 failure/continuity/recovery/isolation. P07 real transport
and browser observations remain explicitly required. Restore PostgreSQL under
#102 only after checkpoint preparation; #97, #92 and #109 remain mandatory before
#108/#48 closure or supported promotion. A merged preparation is not a passed
journey checkpoint.
# Protected delivery appendix: PR #185

Protected squash `de45c67216d7ad0e67417fecb83e02e84486afca` merged at
2026-09-19 12:17:07 UTC. Its tree
`792f2051e00752f39925521c684e6d1958ec326c` exactly equals certified head
`17ed6724fb22c510e70078fa0a955b7b2a6b873a`.

All eight retained local gates passed in 343.880s, completing at
2026-09-18 22:54:13.7248627 UTC; 11,241 units passed in 62.78s and 103 frontend
cases passed. Receipt v4 reports `postgresql_deferred`, zero database instances,
and null combined coverage/timing headroom. Receipt SHA-256:
`BB8DE21A87840FC474F18AC67523BF1E541F1CBC3118FBA7787E0891DDA63215`.
The receipt, plan, unit report and two distributions were copied to the ignored
`programme-release-17ed672-deferred` evidence directory and all five hashes matched.

Hosted run 35404120378 passed: relevant quality 12m36s, units 2m19s and PR gate
3s. All three exact-head CodeQL configurations were processed without findings,
errors or warnings. Fully enumerated reviews, conversations and closing references
were empty; mergeability was clean. No acceptance parent was closed. PostgreSQL
and real HTTP/browser/print/human acceptance remain deferred or outstanding.

The clean separate main worktree fast-forwarded to the exact protected result.
The unrelated repair worktree remained clean at
`aa1ede69fb880dcb12d627bbfc049fba23166e68`. Only agent-owned unfinished P08 files
were preserved in a named stash and restored onto the next branch from main;
the recoverable stash was retained. No deployment or profile activation occurred.
