# Programme operator entry and preceding protected delivery

Date: 2026-09-15. Partial delivery under #108; #108 and #48 remain incomplete.

## Verified preceding outcome

PR #142 merged normally at 2026-09-15 19:41:11 UTC as protected squash
`144ed2325e97a2ae432d470bf5b10ec912b6e6a9`. Its tree
`81789c3668293f5625630284c4b0ed3d935b929c` equals certified head
`09aa37aa594af754a1c9cb75240eb4f1a36ce7ea`. Clean local main and origin/main
were synchronized; unrelated worktrees were preserved.

Exact-head development certification passed all eight retained gates in 378.419s,
including 8,341 units in 64.20s and 93 frontend tests. The schema-4
`postgresql_deferred` receipt has SHA-256
`ea3c1723bc99f08496d6c2b4a2344f40c4dd4c6a5cedf74a57174a6e2b67662b`.
Receipt, plan, JUnit and both distribution packages were archived with matching
hashes outside `.local-ci`. No PostgreSQL instance, combined coverage or native
timing headroom was claimed.

[Hosted run 35013804091](https://github.com/martonpornoi/maru/actions/runs/35013804091)
passed quality in 11m22s, documentation in 9m17s and units in 66.96s (1m22s job).
Exact-head PR gate and CodeQL passed; review/conversation pages were empty and
mergeability was CLEAN immediately before match-head squash. No rerun, timeout
increase or bypass. The delivered Applications/Programme navigation changes no
schema, writer, current profile, production route or deployment.

## Recorded next scope

[Operator-entry scope](https://github.com/martonpornoi/maru/issues/108#issuecomment-5686951216)
belongs to the existing #108 output/continuity connection obligation. Implement
ordinary owner-labelled room, Department and edition selection without UUID
discovery or unrelated directory permission. Reuse exact operator targets and
canonical run-sheet/now-next readers. Default owner fields remain independent;
adopted Department work requires Workforce membership, not its detailed layer.

## Implemented operator entry

The dormant shared-shell entry lists independently authorized exact room,
Department and edition purposes. It shares ordinary operator policy dispatch,
requires all default owners and adopted Department work membership, and reads
minimal labels only after the complete policy observation. Complete bounded
sources and labels are compared before audit and after rendering. Empty admission
reveals no hidden target or count. Optional shared-navigation admission reads no
names/output and creates no new sensitive-read audit.

Run-sheet/now-next links retain their canonical readers. A separately admitted
return to the selector begins a deliberate different-scope choice without extra
layers, while same-purpose output links retain selected layers. No writer, schema,
profile literal, production route or signing trust is changed.

## Development evidence and limitations

- Complete database-free feedback before the final return-link tests: 8,423 passed
  in 64.53s, three existing URLField warnings. Focused selector/owner/navigation:
  109 passed in 1.60s. Final output/entry checks: 68 passed in 1.88s.
- Strict mypy passed all four new/changed principal source files. NumPy docstrings
  and semantic documentation passed after correcting the directly raised alias
  documentation and a generated-sounding summary. The initial module-style
  pydoclint invocation was invalid; its proper executable subsequently passed.
- The initial dormant-route test expected Resolver404 but encountered Django's
  admin catch-all. The corrected assertion verifies neither the operator route
  name nor actual view is mounted; no production route was added.
- The later full feedback run reported two failures and 8,425 passes in 66.06s:
  older notice tests assumed two links and exhausted their two-link policy mock.
  The new selector adds a third link. Strengthened assertions now check all three
  destinations still use the sender rather than a selected recipient, and late
  loss of all three never redispatches preparation. Focused affected contracts
  passed 131 tests in 2.82s. Entry also avoids unrelated notice-policy work.
- Actual entry query/metadata, shared navigation and HTML were exercised through
  synthetic owner/policy/output seams at a fixed synthetic clock. Room selector →
  run sheet → now/next → selector succeeded. Department-only and edition-only
  choices remained separate; an ungranted role saw explicit empty entry without
  room/venue names. No extra layer was selected.
- At 1280 CSS pixels, selector client/scroll widths were both 1280, with one
  H1/main/Access. Enter opened Access with SUMMARY focus and `:focus-visible`.
  The fixture prohibited every SQL connection, non-GET request and signing request.
  Only the task-owned browser tab/server were closed/stopped afterward.
- Four new native cases maintain real room/Department/edition grants, independent
  owner absence, metadata without audit, and actual labelled entry audit. They
  remain uncollected/unexecuted #102 debt. Worst-case bounded metadata/query cost,
  source races and representative cardinality still need native measurement.
- Full widths, native zoom/print, screen reader, actual role comprehension and
  integrated source-change/offline use remain #92/#109 evidence, not inferred from
  this component rehearsal.

Final complete database-free feedback passed **8,428 tests in 65.40s**, with the
same three existing warnings. Whole lint/format passed (1,292 files); documentation
validation passed 565 Markdown files, four repository skills and 215 stable
requirements. A fresh isolated browser pass repeated selector → run sheet →
now/next → selector with the final explicit layer-reset explanation. Its tab and
server were also closed/stopped without touching unrelated resources.

Clean exact-commit certification and protected hosted acceptance are pending.
PostgreSQL suites remain skipped;
the safe-file schema-only question remains unanswered. #102 native, #97 recovery,
#92 genuine human and #109 integrated acceptance remain mandatory before promotion.
