# Guided Programme Work-notice selection

Date: 2026-09-15. Issue #108 inside #48, branch `codex/programme-work-notices`,
based on protected PR #134 squash `bdb0fe356285580663c936c3a18b666e499403d9`.
Partial delivery; #108 and #48 remain incomplete.

## Outcome and contract

The recorded notice connection now offers current labelled occurrence selection,
then owner-authorized operative Work choices. Current Shift title, current eligible
holder, claim/confirmation, explicit-offset accepted interval and current/retained
lineage make the exact commitment readable without an account directory. Retained
work is neither cancellation nor acceptance of its replacement. The existing
preview/preparation, independent review, manual handoff and genuine-person
acknowledgement commands are unchanged. Operator selection and later continuity,
reference/file, setup and integrated gates remain subsequent recorded increments.

Requirements OPS-009, SCH-010/012, AUD-001, PRI-001/008 and UX-007/008/029;
ADR 0102's separate actual-sender/recipient authority and ADR 0100's temporary
database deferral remain unchanged. No schema, migration, runtime privilege,
profile, production route, CI policy, provider or lifecycle change. Rollback is a
revert of this dormant query/form adapter; immutable notice and Work evidence is
unchanged. No physical or logical recovery acceptance is inferred.

## Owning boundaries and failures

Shared Scheduling/Programme source discovery authenticates current pointer,
withdrawal and complete bounded current labels without historical geometry.
Work selection does not require Programme host-roster authority. A public internal
Workforce query separately admits `shift_demands`, `coverage_states` and
`holder_display_labels`; the exact native recipient proof keeps its existing
two-field contract. Neither broad Shift overview nor personal output is reused.

Complete current/retained binding lineage, scoped Demand/Position/Department
parents and accepted interval/version coherence are mandatory. Selection is
complete-or-unavailable at 1,024 operative commitments, fitting Identity's bounded
batch lock. Canonical owner scope precedes the entire sorted sender/holder union.
Missing accounts fail; inactive/unverified/non-person holders are omitted without
hidden IDs/counts. Current lineage, work, labels and sender authority are repeated;
both positive and empty results require actual-sender minimized sensitive-read
audit in existing Workforce-personal retention. No private availability,
qualification, instructions, contact or unrelated work is loaded.

Native preview receives original exact release, occurrence and commitment, not a
selected person or substituted successor. Rendering repeats the full observation,
including accepted intervals and source/person/binding versions. Denial is 404,
malformed input 400, moved observation 409 and incomplete/dependency/audit failure
503, without private fallback. Source and Work overflow have specific recovery
wording; the notice inventory's release filter is not suggested for these pickers.
Visible preview never grants preparation and GET never dispatches a writer.

## Verification actually performed

- 126 focused database-free owner/composition/form/HTTP cases passed in 1.77s.
- Complete database-free suite: **7,717 passed in 46.69s**, three existing Django
  URLField transition warnings. No PostgreSQL collection or execution.
- Strict mypy on four changed owner/transport/form modules and NumPy docstrings
  passed. Repository Python Ruff/format and documentation validation passed.
- One initial real-form test selected the shared-shell logout form rather than
  the target chooser; it was corrected to locate the form by its labelled target
  control. Initial type-check failures from overly broad scope-dictionary typing
  were fixed with an exact two-key TypedDict; no policy or check was weakened.
- In-app browser with actual HTTP/views/forms/templates at 1,280 CSS pixels,
  synthetic owner/identity/policy substitutes and forbidden database connections:
  inventory → current occurrence → retained work → suppressed exact preview.
  Organizer sees separate preparation, read-only role does not; denied role sees
  no private choices/package. The Work chooser has one H1/main, unique IDs and
  width 1,265 within 1,280px. No persistent command was executed. Fixture and tab
  were stopped/closed afterward. This is not real-owner or full UX acceptance.

The existing native scenario
`test_department_without_room_gets_its_linked_work_and_retained_predecessor_only`
now also checks guided operative Work selection equals the exact recipient proof,
does not include removed predecessor commitments, uses explicit offsets, writes
mandatory sender audit and excludes availability/Registration/Participation SQL.
It is maintained but **uncollected/unexecuted** #102 debt. Real lock/concurrency,
scope/revocation and retained-work database proof remain #102/#109; no native
weights, timings, coverage or correctness are invented. #92 retains full
responsive/zoom/keyboard/screen-reader and two-human comprehension acceptance.

Fresh exact-commit certification and hosted protected acceptance are pending at
this implementation checkpoint. Subsequent delivery evidence must name the actual
candidate and protected squash; no earlier receipt can certify this tree.
