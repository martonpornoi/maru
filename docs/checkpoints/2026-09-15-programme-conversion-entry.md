# Direct Programme conversion entry

## Bounded outcome

#108's existing Applications/shared-navigation increment adds a dormant direct
conversion-only task. The exact Department Applications capability, both adapter
pins and independent Programme edition item-management policy precede Department
labels. No decision/review or Workforce directory grant substitutes for either
owner. Both policy sources are explained. No accepted source, private title,
creation cursor or command is read or dispatched by entry.

Programme owns a minimized typed complete policy reference using its real default
adapter and exact Identity/Events owner facts; no new substitute-authorizer seam.
Ordinary absent permission or missing adapter omits conversion only. Malformed
policy, mismatched identity/tenant/lifecycle, dependency/audit failure and changed
source withhold the complete response. The repeated server-only fingerprint
includes both adapter results and Programme owner/decision evidence, even when
the visible list is unchanged. Destinations authorize again.

The six original tasks remain independent. Current profile literals, production
routes, schema, writers and existing conversion semantics remain unchanged.
PRG-008, existing intake/field/audit/shared-shell requirements and ADR 0086 apply;
no ADR is superseded. Read-only failure remains safe retry, not command redispatch.

## Verification in progress

Complete initial database-free feedback passed 8,300 tests in 64.85s, with three
existing URLField warnings. Strict types and NumPy docstrings passed both owner
files. Focused owner/entry/HTTP checks passed 129 cases before the final HTTP
composition cases. One new assertion mistakenly included the shared shell's
logout form; corrected to the page's main landmark, all 29 HTTP cases passed.
Final complete database-free feedback passed 8,303 tests in 64.59s. Whole lint and
formatting passed 1,286 files; documentation validated 563 Markdown files, four
repository skills and 215 requirements. Semantic docstrings passed 693 sources.
Clean exact-commit certification and protected acceptance remain pending.

Four additional maintained native scenarios exercise actual persisted dual grants,
Identity/Events/Department owner queries, ordinary real policy, required audit and
no Programme call/proposal or event/outbox side effects. Only not-yet-adopted future
profile/adapter pins are substituted. Existing three entry scenarios now expect
seven purpose audits. These native tests were not collected or executed;
[#102 debt](https://github.com/martonpornoi/maru/issues/102#issuecomment-5685847250)
retains verification, full coverage and measured timing obligations under ADR 0100.

## Synthetic browser evidence

An isolated loopback fixture used the actual entry query, HTTP, templates and
conversion queue/form rendering with synthetic owner/policy and conversion-source
seams. Every database connection and non-GET request was forbidden. A conversion-only
role saw one labelled task and both policy-source explanations, with no reviewer or
decision links. Enter opened the real accepted-source queue, then a labelled exact
source opened the explicit blank conversion form with its separate confirmation.
No form was submitted and no mutation/persistence acceptance is claimed.

Missing Programme permission and missing adapter each produced the truthful empty
entry without a Department label. At 1280 CSS pixels, document client/scroll widths
were both 1280 with one H1, one main and one Access disclosure. Home-to-Access Tab
had visible focus. Only this task's fixture tab/server was closed/stopped.

This is not actual profile activation, native-policy/persistence acceptance,
all-width/real-zoom/screen-reader or representative human proof. #92 and #109 retain
those integrated and [human tasks](https://github.com/martonpornoi/maru/issues/92#issuecomment-5685847644).
#102, #97, #109 and #92 still gate final promotion;
partial delivery does not finish #108 or #48. Safe-file schema-only authority is
still unanswered and no PostgreSQL exception was assumed.
