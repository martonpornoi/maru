# Guided Programme host-notice selection

Date: 2026-09-15. Issue #108 inside #48; branch
`codex/programme-notice-connections`, based on protected PR #133 squash
`13a51848c78330b636cf35d9f65e3916eb2a136a`.

## Outcome and boundary

The existing notice-connection obligation now has a first owner-specific guided
path: choose an independently labelled current occurrence, choose its current
confirmed host, and preview the existing exact source/relationship package.
The normal preparation, independent review/rejection, manual handoff and genuine
recipient acknowledgement commands remain unchanged. Fixed, independently admitted
notice workflow links connect the existing dormant Programme owner workspaces.
Work/operator selections and their remaining continuations are subsequent increments;
partial delivery, with #108 and #48 still incomplete.

OPS-009, SCH-010/012, AUD-001, PRI-001/008 and UX-007/008/029 retain their existing
contracts under ADR 0102. No migration, runtime role, profile, production route,
provider, schema-only exception or CI policy change. ADR 0100 still defers native
execution; activation/pilot remains fenced by #102/#97/#109/#92.

## Implementation and failure behavior

Scheduling owns bounded occurrence and authentic pointer metadata, including the
latest withdrawal without retrieving old geometry. Programme independently admits
and audits complete current working labels and one deliberately selected item's
host roster. Item/roster versions agree; current people and confirmed relationships
alone become host choices. No person directory, contact, invitation, availability
or historical public-copy discovery is added. Ordinal occurrence distinctions are
current choices, not released timetable labels or times.

Preview repeats the exact affected occurrence and host-purpose proof. Original
hidden release/relationship references, pending digest/version/key and explicit
rationale are not refreshed on submission. The renderer repeats actual owner reads
and compares complete protected facts before releasing bytes; changed labels,
people, pointers, inventory or evidence suppress the private result. Every action
has final independent admission. This never dispatches a command a second time.
Optional moved workflow links are omitted using the original forms.

Denial is non-disclosing 404, malformed closed input 400, moved sources 409 and
incomplete/audit/dependency failures 503. Empty source/host inventories explain
absence without claiming notification completeness. Read-only preview exposes no
preparation authority. Rollback requires reverting this dormant adapter change;
existing immutable notice evidence and owner lifecycle rules are unaffected.

## Verification actually performed

- Existing notice HTTP cases: 35 passed; expanded focused selection/HTTP/navigation
  set: 83 passed before the last two navigation cases were added.
- Complete database-free unit feedback after those additions: **7,661 passed in
  46.54s**, three existing Django URLField transition warnings.
- Targeted Ruff, strict mypy and NumPy docstrings passed after resolving one
  inferred empty-tuple annotation and the new query's explicit Raises section.
- In-app browser, 1,280 CSS-pixel viewport: actual shared-shell views and forms,
  substituted synthetic owner data/policy, database connections forbidden. Followed
  inventory link to current occurrence to confirmed host to suppressed preview;
  organizer sees separate preparation rationale, read-only role does not. Denied
  role sees no private package or host labels. Preview has one H1/main, unique IDs,
  document width 1,265 within the 1,280 viewport. No persistent command was executed.
- Initial fixture lacked the shared shell's admin namespace and failed to render;
  its isolated URL configuration was corrected, then the actual journey succeeded.
  The task-owned fixture and browser tab were closed after rehearsal.

The existing native scenario
`test_personal_presence_is_exact_released_work_not_full_envelope_or_public_copy`
now includes scoped guided source/host composition, required read audit and excluded
geometry/availability/Registration/Participation SQL assertions. It was **not
collected or executed**; no native weight, timing, coverage or correctness claim.
Further real-owner role/revocation/withdrawal/concurrency proof remains #102/#109.
Full responsive, keyboard, screen-reader, genuine zoom and two-human understanding
remain #92. No broad visual or accessibility acceptance is inferred from DOM facts.

Fresh exact-head certification and hosted protected acceptance are pending at
this implementation checkpoint. Later delivery evidence must name the actual head.
