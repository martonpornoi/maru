# Applications and Programme workspace connections

## Previous protected outcome

PR #141 delivered direct conversion-only entry at protected squash
`1caf776eb0067f5fef11251a942c261b534d26cc`, 2026-09-15 18:59:51 UTC. Exact certified
head `07ad81ed7ebd05c2d6386384fe56be69286bc3ff` and squash share tree
`da747fa7827c77bdb2f38724a9457b0c2887642e`. Normal match-head squash followed
green PR gate/CodeQL and clear reviews; clean main and origin/main synchronized.

All eight retained local gates passed in 375.394 seconds (6m15s), with 8,303 units
in 64.15s and 93 frontend cases. Schema-4 receipt result was `postgresql_deferred`,
zero databases and null native coverage/timings. Receipt SHA-256:
`7f8c497c1eaec5a5356a7fbe6357dc78c4077d5c5504eafe86a24fff6fa3542e`.
Receipt, plan, JUnit and both package artifacts were archived with verified hashes.

Hosted workflow `35008833491` passed units in 99.60s (2m03s job). Quality took
18m33s (18:39:48–18:58:21 UTC), including documentation in 16m01s
(18:41:09–18:57:10 UTC). This is observed 11m27s margin under the unchanged
30-minute quality limit, not a guarantee. PostgreSQL remained skipped; four new
and three changed native scenarios remain #102 debt. #108/#48 remain open.

## Current bounded connection

The recorded #108 increment connects the dormant Applications edition entry to
the existing item, timetable, release and notice workspaces and provides an
independently permitted return. Reuse shared reverse/resolve-verified navigation,
not a second registry. Applications owns metadata-only admission reusing the same
seven fixed purposes and complete owner/policy/adapter proofs without label or
application-record lookup. Actual destinations authorize again.

Optional link movement/failure omits continuations without changing retained input
or completed commands. Labelled entry remains complete-or-unavailable with required
audit. No current profile, production route, schema, writer, private-content grant
or activity analytics changes. Accountable setup, operator purpose discovery,
safe files and #102/#97/#109/#92 acceptance remain separate.

## Implemented safeguards and verification

The Applications labelled catalog and metadata hint now share the same private
complete owner snapshot. Metadata mode skips label lookup entirely and writes no
manufactured sensitive-read/activity audit. It accepts no public substitute
authorizer. Ordinary missing, unavailable or changed authority omits only an
optional fixed-label hint; the actual labelled catalog retains strict denial,
required audit and final complete-source comparison. The existing shared navigation
admits the Applications route independently and checks exact reverse/resolve.

The entry reciprocates with independently admitted workspaces and removes changed
links by rerendering before the final protected-source comparison. Existing item,
planner, release, notice and Shift consumers reuse their prior post-render guard.
Original input and saved command semantics remain unchanged.

Complete database-free feedback passed 8,341 tests in 65.85s, with three existing
URLField warnings. Focused entry/navigation passed 148 in 1.67s. Strengthened
item/release regressions passed 146 in 2.05s, including disappearing entry links
around original and replayed receipts and failed-command input. Strict mypy passed
all three changed source files; NumPy docs, whole lint/format (1,286 files), semantic
docstrings (693 sources), and docs validation (564 Markdown, four skills,
215 requirements) passed. A new zero-UUID metadata test initially found an
unnecessary owner lookup; metadata now rejects zero identifiers before dispatch.
The initial missing optional-principal narrowing was corrected and strict types
rerun successfully. No test was removed or skipped to obtain these passes.

The four existing real-grant native conversion-entry cases additionally exercise
metadata admission without new audit rows before the separately audited labelled
query. They remain [uncollected/unexecuted #102 debt](https://github.com/martonpornoi/maru/issues/102#issuecomment-5686691508).
No native timing, weight, success or coverage claim is inferred.

## Synthetic browser evidence and remaining gates

Real Applications query/metadata, shared navigation and actual item/timetable/
release rendering were exercised over synthetic owner/policy/source seams. All
database connections and non-GET requests were forbidden. Ordinary links completed
Applications → items → Applications → timetable → Applications → release →
Applications. Reviewer-only and conversion-only entries had only their own task,
without planning links; the item-only page had no Applications return.

At 1280 CSS pixels, Applications had one H1/main/Access and client/scroll widths
both 1280. Home-to-Access Tab showed visible focus. Only this fixture tab/server
was closed/stopped. No form was submitted and no fresh native discard/zoom,
screen-reader, notice/Shift browser or native-persistence acceptance was claimed.
Their mappings are maintained in automated tests; integrated roles, full widths,
human understanding and assistive technology remain
[#92 work](https://github.com/martonpornoi/maru/issues/92#issuecomment-5686691829).

Fresh clean exact-commit development certification and independent protected
hosted acceptance remain pending. This does not finish #108/#48 or authorize a
profile, director pilot, deployment, PostgreSQL test run or schema-only exception.
