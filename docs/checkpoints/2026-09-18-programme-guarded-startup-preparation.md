# Programme guarded startup preparation

Date: 2026-09-18
Scope: #108 beneath #48; EVT-006/007, NFR-013, ADRs 0081, 0100 and 0106.

Prepared isolated WSGI construction using strict base-derived settings. Explicit
pre-model candidate registration and unchanged current manifests precede real
joined internal Effects handlers. Existing notification handlers remain intact;
no candidate notification destination is invented.

An independent isolated compatibility contract invokes all ordinary Django
checks. Only the exact known owner functions with exact expected dormant errors
are accounted for, after separately checking the original profile snapshots and
real candidate catalog. Unknown warnings/errors, missing checks, changed messages,
manifest drift and handler conflicts fail closed. The three read-only owner
validators now accept optional snapshots; default registered production checks
are unchanged. This is not a blanket ignored-error list or production exemption.

Before application construction, require actual scoped runtime identity, candidate
migration history, exact physical profile constraint and genuine health/provenance,
setup and role readiness. The literal PG17 deparse expectation remains unobserved
and must be confirmed under restored native acceptance. A mismatch refuses
construction. No readiness probe, authority, approval or session is substituted
in runtime code; unit tests use explicitly labelled doubles only.

Focused database-free feedback passed **130 cases in 0.62s**. Complete feedback
passed **10,513 cases in 59.79s**, with three existing Django URL-field warnings.
Exact-head certification and hosted protected delivery are pending separately.
The first certification attempt stopped at NumPy docstring validation because
the new optional snapshot parameters used `optional` rather than the required
exact `default=None` notation. Corrected the docstrings without changing behavior;
the corrected commit must receive a fresh complete retained certification.
The maintained candidate native provisioning case additionally tests rejection
without genuine provenance activation. All four provisioning and four transport
host-only cases remain uncollected/unexecuted. No Docker, database, schema-only
check, server, browser journey or production activation was performed.

This component does not supply the owned serving runner, real setup/role scenarios
or positive native startup/P01–P12 evidence. Prepare those next, then restore the
tracked PostgreSQL policy through protected delivery before native acceptance.
Human #92, recovery #97, PostgreSQL #102 and integrated #109 remain final gates;
#108/#48 remain open. The user authorized that sequence, not bypassing its gates.

## Protected delivery

PR #168 squash-merged as `cc4cb08077ba4cc7b167fa798abb1050c28915db` at
2026-09-18 15:07:43 UTC. Corrected certified head
`6a5767eea6ba17dbbc104c71b0933a8277580232` and protected squash share tree
`ac7df27a44bc8ffc9beaa5fc128c08066294b408`.

All eight retained local gates passed in **325.186 seconds**, including
**10,513 units in 60.21 seconds** and **103 frontend cases**. Receipt v4 reports
`postgresql_deferred`, zero database instances and null combined coverage/headroom.
Five artifacts were hash-verified in ignored local archive
`.tools/certification-evidence/issue108-6a5767e-deferred/`; receipt SHA-256:
`1022F2E1DBFBA79EE6A3243CF891A8D96455C8827E1451E65C719E86F2E10D1F`.

Hosted run `35358994193` passed quality in **11m57s** (14:53:22–15:05:19 UTC),
units in **1m50s** (14:53:22–14:55:12 UTC), and PR gate in three seconds
(15:05:22–15:05:25 UTC). All three CodeQL categories from run `35358990499`
were processed at the exact head without error. Immediately before ordinary
match-head squash, the PR was CLEAN/MERGEABLE with no reviews, unresolved threads,
pagination or automatic issue closures.

The clean separate main worktree fast-forwarded to the protected result and
matched origin/main. The detached issue-96 repair worktree remained untouched.
#108 records only this increment complete; #108/#48 remain open. Maintained
native debt is recorded on #102, comment `5731799693`.

The next fixture runtime/setup increment must explicitly project minimum native
table privileges: canonical production grants deliberately keep these dormant
relations SELECT-only. This prerequisite is recorded under #108, comment
`5731847223`, and belongs to its existing complete-fixture item. It does not
authorize production activation or owner-role application requests. Continue on
`codex/programme-fixture-foundations`; this later handoff is not certified by the
earlier receipt.
