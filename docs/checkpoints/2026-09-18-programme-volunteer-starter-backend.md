# Programme Volunteer starter backend preparation

Date: 2026-09-18. Scope: bounded backend increment of #175, native child of
#108 within #48. This checkpoint does not complete #175 or either parent.

## Contract and implementation

HR-012 and ADR 0107 retain each actual person's own decision about the single
fixed shared `workforce-volunteer@1` definition. The separate Workforce-owned
request and terminal decision are necessary for durable pending recovery,
independent approval, cancellation, expiry, serialized outcomes and exact retries.
No generic template builder, new role recipe or legacy Workforce-only redesign
is introduced. Selecting an email cannot approve Programme template publication.

The author requests exact organization/series/Programme-edition scope, original
named different approver and bounded rationale. The approver personally approves
or declines; only the author can cancel. Current ordinary verified people need
Organization role management and Edition structure authority backed by actual
control-source selection. Approval rechecks both people, source authority and
seven-day deadline under canonical locks. New intent and publication require
Draft/Preparing. Exact retry reauthorizes the actual retrying actor and returns
original evidence without creating planning state or reissuing a template.

Atomic approval uses the existing private Workforce factory and public canonical
RoleBundle command, or explicitly reuses exact compatible historically proven
meaning. It creates no Position, assignment, Shift, Participation, Registration,
payment or unrelated communication. Native output attribution retains original
author, actual approver and canonical independent audit operation. Conflicting
reserved meaning or later failure aborts the transaction.

Workforce migrations 0025–0027 add two protected-reference relations, exact scope,
person, definition, audit and output guards, one terminal result, append-only and
anti-truncate protection. Separate reverse fencing locks both relations before
checking emptiness; retained intent requires fix-forward/consistent recovery.
Production runtime sees only SELECT. The candidate alone explicitly adds
request INSERT/UPDATE for row locking and decision INSERT, extending its table
inventory from 82 to 84. Its actual startup adds genuine starter readiness.
The fourteen reviewed helper grants and current profiles remain unchanged.

The canonical ACL artifact gains only the two new read-only exceptions; its
test-only candidate source pin becomes
`3d5bf2c0ce58b274344ff82219bbd853e078708157488adda42cfca82a0b29a8`.

## Authorized schema-only observation

One task-labelled loopback PostgreSQL 17.11 Alpine container, tmpfs-only storage,
1.5GiB memory and two CPUs, applied empty forward migrations under a ten-minute
outer deadline, 120-second statements and ten-second locks. It finished in
**160.110 seconds**. All native catalog flags were true; no profile, workflow,
provenance activation, application server or PostgreSQL test was executed.

Observed exact relation fingerprints:

- `workforce_programmestarterdecision`:
  `d4668b5dbedec80f0b5e62ca09f00db5eed6dc8e836164d15d649c207f91af6d`.
- `workforce_programmestarterrequest`:
  `a4d2395e540d1b93445a668cef853ce5a7399138bda17391f9dd75d36f120a55`.

The subsequent read-only check verified both tables empty and actual pinned
readiness true. The exact container identity, task/purpose labels, tmpfs and no
mounts were verified before stop/removal. Its empty disposable database is gone;
no other containers, volumes, user data or production resources were changed.
This observation is not native command, rollback, concurrency, runtime-role or
recovery acceptance.

## Verification and remaining delivery

Database-free inputs/commands/boundaries/startup feedback passed 165 tests in
0.93s. Schema/model/runtime focused feedback subsequently passed 121 in 0.67s.
Strict targeted mypy passed and semantic Python documentation passed 745 sources.
Initial complete feedback found one stale expected table inventory; the expected
two explicit SELECT-only relations were added, without weakening the comparison.
Corrected complete feedback passed **11,057 cases in 62.98 seconds**, with three
existing Django URL-field warnings. Full strict mypy passed 724 sources and
repository documentation validation passed 633 Markdown files, four skills and
215 requirement identifiers. Exact-head certification remains pending at this
snapshot; retained evidence must describe the actual certified commit.
The subsequent full run also caught the new native file missing from the
diagnostic file inventory. Its 29.955-second entry is the existing median
placeholder, not a measurement; authoritative group scheduling retains its
conservative unmeasured fallback. No native timing or headroom is claimed.

`tests/integration/test_programme_starter.py` maintains actual two-controller
request/approval/reuse, no-assignment output, nonapproval, wrong-person denial,
expiry, physical late-failure rollback, direct-mutation refusal, native drift and
unused/used guard-contraction cases. They are **uncollected and unexecuted** under
#102. The transaction-local schema candidate remains intentionally incomplete;
it is not a supported manifest, genuine runtime-role fixture or P01–P12 acceptance.
Concurrent request/decision/revocation and complete runtime matrices must be
completed and executed with the final native gate, not inferred from mocks.

Next: audited bounded own-person queries, original-person signed preview/confirm,
shared-shell own-approval task, Positions/setup entry points and actual fixture P06
composition. Keep #175 open until those complete. Human comprehension belongs to
#92; PostgreSQL restoration/#102, recovery #97, integrated #109 and supported
promotion remain required before #108/#48 closure. No production activation.
