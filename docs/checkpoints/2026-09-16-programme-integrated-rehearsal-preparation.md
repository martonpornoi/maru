# Programme integrated rehearsal preparation (#108/#109/#92)

This documentation-only increment prepares one joined Programme acceptance
protocol after protected PR #150. It does not implement accountable setup,
activate a profile, run a database, or claim native, human or integrated success.

## Outcome and inspection

The new [rehearsal protocol](../operations/programme-integrated-rehearsal.md)
maps twelve checkpoints to the existing #48 outcome: accountable setup, proposals,
review, accepted/core items, planning, staffing, release, published changes, on-site
operation, outages, recovery/stop-use, and cross-scope/excluded-effect proof.
It defines distinct sessions, human role cards, native browser/accessibility
tasks, exact-candidate evidence, failure records and safe cleanup. The operator
index links it, and the maintained setup contract and roadmap no longer describe
delivered Scheduling and continuity components as absent.

Read-only inspection confirmed Events currently owns Workforce setup rather than
a generic Programme setup command. Organizations pins existing Maru-operator and
Executive Board representation definitions; Workforce owns Department creation.
The Programme successor must preserve those public owner boundaries, immutable
existing roles and two genuinely accepted person appointments. No private-model
copy, direct lifecycle write or fixture authorization substitution was added.

Existing component rehearsal files are not the complete fixture and some require
PostgreSQL/authority substitutions. The protocol names those limits rather than
offering a launch command that could violate the current deferral or imply joined
acceptance. Setup schema-only observation remains separately unapproved.

## Verification and remaining work

Run documentation validation, complete inexpensive database-free feedback and
fresh exact-commit deferred certification before protected delivery. Bind actual
results to the PR and its exact head; this preparation checkpoint is not a receipt.
Initial unit feedback passed 8,947 cases and failed one existing documentation-policy
assertion because the rewritten status omitted its explicit current-profile
writer/effect-route prohibition. That prohibition was restored, not the test
weakened; final passing evidence must come from the corrected candidate.
Corrected feedback passed all 8,948 units in 57.20s with three existing Django
URL-field warnings. Documentation validation passed 585 Markdown files, four
repository skills and 215 unique requirement identifiers; `git diff --check` passed.
No application behavior changed, so no behavior tests or database test inventories
were changed. No PostgreSQL tests were collected/run, no schema observed, and no
new browser or human acceptance was performed by writing the protocol.

The carried PR #150 delivery checkpoint records already verified component
evidence, separately from this new preparation. #108/#109/#92 and #48 stay open.
Finish accountable setup and the actual isolated fixture; then restore #102
database verification, resolve #97 recovery, collect #92 human evidence, finish
#109 joined acceptance and perform #108's separately verified profile promotion.
No schedules, deployment, unrelated cleanup or production data are authorized.
