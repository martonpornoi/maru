# Programme stopped foundation and worker preparation

Date: 2026-09-18

## Outcome and scope

Continue #108's complete isolated fixture from protected PR #169, without changing
production profiles, role policy, ACLs or ADR 0100's PostgreSQL deferral. NFR-013,
IDN-001/002/004/005/011/012/014, AUD-001 and ADRs 0044/0081/0100/0106 remain the
owner boundaries; no real controller/legal approval or deployment is implied.

Prepared run-scoped ephemeral secrets and a fixed stopped bootstrap that composes
real Identity/Authorization owners before candidate grants. A separate genuine
runtime worker cycle composes existing delivery/expiry/retention commands and
demands actual native readiness. These are source preparations, not executed
activation, startup or complete Programme acceptance.

## Preserved safety

- Policy/opt-in precede generation; no resource starts while deferred. RSA-3072
  invitation encryption, HMAC and session material and random administrator
  password remain separate. Only the worker receives the private delivery key;
  only the stopped owner receives the bootstrap password. Web construction rejects
  even empty owner/worker secret variables.
- Fictional one-day `SYNTHETIC` retention with `isolated-fixture-only` approval is
  fixture configuration, not a real convention's legal schedule. Actual owner
  activation and heartbeat clocks are unchanged.
- Bootstrap requires genuine same-run migration identity, PG17, strict settings,
  autocommit, canonical loopback HTTPS origin, candidate migration and fresh
  account/organization/edition/provenance/policy state. Real key/policy parsers
  run before account creation; no worker private key is admitted.
- The genuine platform administrator is created through the Identity manager,
  not a verified-person factory. Actual provenance activation owns its top-level
  transaction and zero-blocker/postflight checks; retention uses its actual owner.
  No person, organizer role, representation acceptance or forged approval is added.
- A partial owner failure disposes the exact owned fixture. There is no retry by
  clearing irreversible evidence. Application/worker activity uses the runtime
  login, never migration/admin impersonation.
- Worker actual identity/role/REFERENCES checks precede the three existing bounded
  commands. Complete independent candidate system checks are performed first;
  ordinary programmatic management-command invocation replaces no owner guard.
  No heartbeat/readiness result is manufactured. Full readiness follows the actual
  cycle. Synthetic in-memory mail is not external delivery evidence.

## Verification actually performed

- Iterative complete database-free feedback: 10,611 passed in 59.75s, before the
  final additional web-secret/origin guards. Three existing Django URL-field
  transition warnings remained.
- Final combined focused feedback: 166 passed in 1.06s, covering actual ephemeral
  key parsers, redaction/separation, policy/process/origin/native-identity refusal,
  owner ordering, partial failure, worker command dispatch and web-secret denial.
- Ruff feedback passed before the final documentation checkpoint; clean exact-head
  retained certification and hosted acceptance are still pending.
- No PostgreSQL test collection, native worker, activation, database/container,
  schema-only check, web server or browser was executed.

## Maintained debt and continuation

Six host-only cases now live in `programme_provisioning_native.py`. The new sixth
case will prove real stopped owner activation/retention and absence of any person,
organization or edition, then actual worker commands and a separately constructed
secret-free WSGI application. Privilege-matrix rows now have explicit ordinality.
These assertions have not been run; they remain #102 debt rather than evidence.

Next finish the owned HTTPS runner, real Identity verification, three foundation
setup modes, separate actual synthetic-person representation/role actions and
P01–P12. Restore tracked PostgreSQL policy through protected delivery only after
preparation, then prove #102/#97/#92/#109 before final supported-profile promotion.
Representative human acceptance remains genuinely human; #108/#48 stay open.
