# Dormant shared Programme navigation

Date: 2026-09-18
Parent: #108 under #48. Base: protected PR #162,
`cf53ccc5c53b645e27d3b76a66425977bf7807e9`.

## Outcome and boundary

Core composes seven independently authorized fixed-label Programme tasks into the
existing shared registry: Applications, items, planning, release, notices, on-site
run sheets and access requests. Events declares seven future shell kinds without
altering either literal current v1 manifest. Exact future profile membership and
actual mounted handler/scope identity remain prerequisites. Platform setup uses
its existing Identity-backed admission, not an operational role shortcut.

Narrow-role edition discovery first projects current name-free authority, bounds
candidate IDs to 256 and independently admits a mounted task before loading labels.
Overflow or unavailable dependencies add no partial additional list. Legacy
discovery, pin storage and personal separation are unchanged. A preference never
preserves a revoked link. No source inventory, new audit operation, mutation,
grant, migration, runtime privilege or production URL is added.

Requirements: EVT-005 through EVT-007, UX-005 through UX-008, UX-019, UX-020,
UX-027, UX-029, NFR-003, NFR-005 and NFR-013. Existing ADRs 0036, 0049, 0055,
0081, 0100 and 0106 apply; no new architecture decision is introduced.

## Verification observed

- Complete database-free feedback: **9,837 passed in 58.73 seconds**, with three
  existing Django URL-field deprecation warnings. This adds 48 cases over the base.
- Strict mypy: **718 source files**. Ruff lint/format passed.
- Tests cover unchanged current/unknown profiles; seven independent kinds; wrong,
  shadowed and absent handlers; exact scopes; narrow candidate discovery and foreign
  rebinding; complete 256/257 bound; unavailable owners; exact-true admissions;
  stable pin/search codes; revocation and personal/management separation.
- First full feedback found a substring collision between a helper named for
  Programme items and the dormant containment scanner. The helper now accurately
  describes all Programme destinations. The containment scanner is unchanged and
  the complete rerun passes.
- A maintained native case in `test_programme_role_schema.py` uses real grant and
  revocation commands, a transaction-local candidate containing only the access
  shell kind, fresh selector requests and actual owner admission. It checks
  labels/links disappear and metadata discovery creates no sensitive-read audit.
  It was **not collected or executed**, under ADR 0100/#102.

## Bounded browser evidence

An isolated leased loopback fixture prohibited all database connections. It used
the real registry, owner-link composition, templates and navigation JavaScript,
with explicit synthetic principal, edition, owner-admission, pin and selector
doubles. It is not a complete manifest or proof of native discovery/authorization.

Observed ordinary search for `timetable` returned planning and release tasks.
Escape cleared search; keyboard entry from the saved access pin reached the actual
scope-chooser view. A narrow admission left only the access pin; revocation removed
it. The personal menu contained only the separate personal destination. At 390px,
opening navigation exposed a labelled modal drawer, focused Close, and Escape
returned focus to Open navigation; the screenshot was inspected. All 320, 390, 768,
958, 1024, 1280 and 1920px pages had one H1/main and no horizontal overflow.
Browser warning/error log was empty. The task's tab and exact fixture processes
were closed and the viewport reset.

Actual 200% zoom, screen-reader/native preference behavior, representative-human
discovery, real persisted pin mutation and native authority remain separate #92/
#102 evidence. This read-only navigation change creates no new form or approval
flow. Existing two-person acceptance is not replaced by synthetic accounts.

## Remaining delivery

Clean exact-commit local certification and exact-head hosted acceptance/merge remain
next at this snapshot. Preserve the receipt outside `.local-ci` before another run.
Then finish #108's complete isolated fixture. #102 restored native acceptance,
#97 logical recovery, #92 human acceptance and #109 joined proof remain prerequisites
to final profile promotion. #108 and #48 remain open.
