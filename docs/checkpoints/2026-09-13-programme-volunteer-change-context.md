# Programme volunteer change-context checkpoint

Date: 2026-09-13. Issue: [#104](https://github.com/martonpornoi/maru/issues/104).
This extends the local purpose-comparison milestone, not protected delivery.

Workforce independently supplies exact-self current/retained Programme binding
references selected only from own commitment demands. Complete lineage is shared
with existing operator reads without sharing their authority. Independent work,
demand and binding versions/state retain predecessor work without substituting
successor demands or exposing private instructions. Unadopted Programme returns
an explicit absent layer without reading work, Programme or Scheduling records.

Scheduling's new non-persistable `scheduling.view_work_self` capability is pinned
by no profile. It independently admits only linked own claims/confirmations to
active/predecessor release context. Only their occurrences select geometry;
ended/unrelated work grants no release discovery. Release removals or time/room
changes do not cancel, relocate, retime or confirm retained work. Owner versions,
both native manifests, final authority and mandatory audit fence disclosure.
No schema, grant, route, delivery or acknowledgement is activated.

## Verification

- Workforce reference: 44 database-free cases passed in 0.47s, including actual
  shared history-aggregation/filter logic against synthetic rows.
- Volunteer comparison, capability and history selection: 108 cases passed in
  0.63s. The native persisted-capability migration test explicitly excludes the
  new non-persistable self purpose; historical migrations were not rewritten.
- Full unit suite: 5,250 passed in 33.52s, two existing URL-field warnings.
  Local JUnit: `.tools/issue104-work-all-unit.xml`.
- Ruff/formatting, strict types, semantic/NumPy docstrings and docs checks pass.
- Existing real Workforce-only and retained Department-work fixtures now assert
  no adoption/discovery, exact self/audit, current/predecessor linkage and no
  commitment changes. PostgreSQL execution remains explicitly deferred by
  ADR 0100 and is debt for #102, not passed database acceptance.

Communication preparation must use sender-authorized owner recipient references,
not call a personal read as another actor. Governed review/manual handoff,
immutable exact-recipient acknowledgement and visible surfaces remain #104 work.
No exact-head certification, #104 PR, merge or browser acceptance is claimed.
