# Programme selected-room and private timetable preparation

Date: 2026-09-18
Scope: #108 within #48; real-owner P05 composition preparation, not native acceptance.

## Contract and boundary

Requirements SCH-001/003/004/007–010, PRG-008, VEN-001/002/008, AUD-001/003
and NFR-013; ADRs 0053/0088/0104/0106 unchanged. This increment changes only
test-only fixture composition and its documentation. No production schema,
profile, runtime privilege, route, policy or architecture change.

The same guarded runtime and private source chain now prepare two real selected
rooms, one service day and three explicit occurrences (core ceremony and two
grouped instances of one accepted item). Separate actual verified people receive
literal scoped recipes via the original controllers' own request/approval.
Shared Venue catalog authority is explicitly Organization-wide; each selected
room is separately discovered and approved at its typed resource scope. No
all-capability role, authorizer substitute, model factory or direct write.

Actual Venue commands create then activate the fictional property, create the
source rooms/configurations, select them for the edition and record hard
availability. Configuration IDs are read from the ordinary authorized Venue
workspace form using real backend authentication and CSRF-enabled in-process
HTTP. The adapter validates private/no-store HTML, exact unique visible choices,
bounded bytes and unchanged admitted route scope, then logs out. It does not
claim browser or TLS acceptance, and does not inspect private model tables.

Actual Scheduling commands retain a deliberately conflicted candidate: two
occurrences contend for one room, capacity is exceeded, and a repeated host is
required in different rooms simultaneously. Saved evaluation plus fresh review
must expose all three blockers with complete current owner sources. An exact
revision copy is independently edited; stale version intent must fail and the
original candidate/manifest must remain unchanged. Corrected evaluation must have
complete available sources and no blockers/unavailable findings. Warnings are
not silently acknowledged, and future checks are not relabelled as passed.
Required host presence is explicitly effective delivery, not inferred room setup.

The private child accepts only four exact bounded inputs, rejects mismatched
source identities and reused people, retains original expiry, and keeps all
credentials off argv/logs/public responses. No private worker keys enter it.
Partial failure requires owned fixture disposal; no success-history rewriting.

## Verification and remaining evidence

Focused database-free feedback: 112 passed in 1.12s. Pure/mocked tests exercise
actual signatures and normalized typed intent, real scoped helper forwarding,
original control versions, exact copy membership, explicit repeats/host presence,
stale-write refusal, complete current conflict reports, authenticated form
discovery and session cleanup, same-source handles and bounded child pipes.
An initial oversized parametrized test name exceeded Windows' environment
variable limit; short explicit case IDs corrected test reporting, without
changing the oversized-input denial assertion. Complete database-free feedback
passed 10,839 tests in 59.96s with three existing Django URL-field warnings.
Ruff and diff checks passed. Exact-head certification and protected delivery
remain pending at this snapshot.

The single host-only composed native case now maintains readback of both exact
candidate manifests and zero actual booking/reservation/approval/release state.
It remains **uncollected/unexecuted**. No Docker command, image pull, actual scanner,
database, migration, schema-only check, actual server/browser or native test ran.
The in-process HTTP path is prepared, not executed by these mocked tests.

Next prepare physical reservation and independent approval plus P06 staffing;
continue publication, changes/notices, on-site outputs, continuity, logical
recovery/stop and every-checkpoint cross-scope/excluded-effect assertions.
Full P05 combinations/pointer/keyboard/forms and other failure paths remain
integrated native/browser evidence. Restore PostgreSQL only after preparation
through its tracked protected policy change, then collect #102/#97/#92/#109
acceptance before final supported-profile promotion. #108/#48 remain open.
