# Programme Volunteer starter own-person review

Date: 2026-09-18. Bounded #175 surface increment inside #108/#48, following
protected backend PR #182. No profile activation, PostgreSQL or human acceptance.

## Outcome and boundaries

HR-012/ADR 0107 now have dormant shared-shell request and review adapters. An
ordinary currently authorized controller previews the fixed Organization-owned
Volunteer definition and a different real controller. A purpose-specific signature
binds original person, author, scope, reason and retry identity. Confirmation
does not re-resolve a mutable email or make the named person's approval.

An audited query exposes only the actor's own authored/assigned requests, complete
or unavailable above 100 pending records, plus exact known historical intent.
Names come from eligible current display labels, not a directory. Actual own
approve/decline/cancel use the backend and recent own-session authentication.
Step-up opens separately; validation/uncertainty preserves original bounded
input, proof and retry key. No replacement is automatic. Terminal output is
history, never an effective grant. Fixed meaning still creates no Position,
assignment, Shift, Participation, Registration, payment or attendance.

Strict input, CSRF, sensitive-parameter handling, private/no-store CSP responses
and post-render repeated owner admission protect disclosure. Stale scope, labels,
authority or terms discard the rendered body. Forms retain ordinary labels,
error focus and the existing dirty-input protection. Positions offers a link only
after the real mounted route and current audited controller admission; unavailable
destinations create no dead link. Setup explains the ordinary-person handoff
without treating platform administration as operational approval. The isolated
joined URLconf adds the eleventh owner route module; production URLs/profiles do
not change. No schema, migration, native source pin or runtime privilege changes.

## Verification and remaining gates

Initial HTML feedback caught four fixture/assertion defects: textarea leading
newline normalization, two RequestFactory duplicate-value encodings and the
production admin catch-all being mistaken for an absent resolver. Corrected
assertions preserve actual duplicate rejection and actual owner-route absence.
Focused HTML/routes passed 434 cases in 1.78s; expanded starter/setup/routing
feedback passed 704 cases in 4.49s. Strict targeted mypy passed seven sources,
semantic documentation passed 753 sources, and Ruff passed. Complete feedback
passed 11,148 cases in 61.50s with three existing Django URL-field warnings;
repository documentation passed 635 Markdown files, four skills and 215
requirement IDs. Exact-head certification is pending at this snapshot.

The existing native starter file additionally maintains actual selected-person
preview, own-reader admission, unrelated-person denial and terminal/open history
behavior. It remains uncollected/unexecuted #102 debt; mocked owners in unit HTML
tests do not certify PostgreSQL, current native authority, concurrency or runtime
containment. No browser, zoom, screen-reader or genuine-human result is claimed.
Human acceptance of independent decisions, step-up recovery, original retry,
keyboard/narrow/200% layout and comprehension remains #92, not waived.

Keep #175 open for the actual blank-setup/P06 composition. Then finish actual
P01–P12 preparation, restore #102 exhaustiveness and timing evidence, and complete
#97 recovery, #92 human and #109 integrated gates before supported promotion and
#108/#48 closure. No production deployment or user data was touched.

## Protected delivery appendix

PR #183 merged at 2026-09-18 21:53:15 UTC as
`180d4e84431d31d044d1b9db716c423431ca88f7`. Tree
`e3ea0f139fb739b70dacbd67dafcd66a0c567234` equals certified head
`2046e8741b39f65e5c6b75dfa2c396fc8925afb0`. All eight local development gates
passed in 345.671 seconds: 11,148 units in 62.78s and 103 frontend cases. The
v4 receipt is `postgresql_deferred`, with zero database instances and null
combined coverage/headroom, SHA-256
`286118d7ae976bba37351e2f104e31ab19e295f45d1a81a75c61c2ee7f598735`.
Five original artifacts were hash-verified into the ignored exact-head archive.

Hosted run 35397854818 passed quality in 12m18s, units in 1m35s and PR gate in
4s. Three processed exact-head CodeQL configurations had zero findings and no
errors/warnings. Review/thread/closing-reference pages were complete and empty;
the merge matched the exact head without bypass. Clean separate main and
origin/main matched the protected result; the unrelated repair worktree stayed
at its prior commit. #175/#108/#48 remain open; #92 retains the new actual-person
and browser checklist. No native, human or production gate was waived.
