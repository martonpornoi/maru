# Public and personal Programme output component

Date: 2026-09-12\
Status: Local implementation and focused rehearsal; protected delivery pending\
Parent: [#48](https://github.com/martonpornoi/maru/issues/48)\
Child: [#99](https://github.com/martonpornoi/maru/issues/99)

## Outcome and boundary

ADR 0097 defines current release-derived public and exact-person projections,
independent owner authority, bounded formats and honest unavailable states.
The branch starts from protected #98 squash
`0bea1b46f8611f3da8a3d3664f258505a8deb7e4`; the
[previous delivery checkpoint](2026-09-12-atomic-release-protected-delivery.md)
owns that exact result. This checkpoint does not predict a #99 commit or merge.

Public output has genuine exact-profile public admission, exact immutable
reviewed Programme copy and current minimized Venue wayfinding. Complete
canonical/native release evidence and final owner/disclosure rechecks prevent
latest-copy substitution, partial output and stale last-good fallback.
The existing host-self query also stops returning withdrawn latest public copy,
without falling back or fetching private reviewer metadata.

Personal output independently composes explicit Programme host purposes,
approved own presence and retained Workforce work. The non-persistable
`scheduling.view_host_self` capability is unpinned in every current profile.
Workforce-only makes no Programme/release or Participation lookup. Actual own
records justify a minimized current Events name/version; empty scopes do not
discover edition names. Required host presence, event phase context, tentative
claims, confirmed work and ended history remain distinct. Current instructions
never silently relocate accepted work or prove attendance.

Closed deterministic typed/JSON and RFC 5545 outputs have row/text/byte bounds,
UTC-aware dates, safe escaping and stable calendar identities. Public and private
payloads are not interchangeable. Every HTML/print/download request authorizes
again; private responses are no-store. Combined private calendar imports are
withheld after host-release withdrawal/invalidation, while JSON/HTML preserves
known state and unchanged work. No imported-file update/erasure guarantee exists.

## Focused evidence and repairs

- The host-copy regression first failed as expected. Eighteen host/copy checks
  and ten narrowed SQL-minimization/privacy checks then passed.
- The public pipeline passed 17 PostgreSQL cases; real four-format withdrawal
  plus existing release-manifest regressions passed 36 cases in 250.36 seconds.
- The Workforce owner query passed 14 PostgreSQL cases in 52.55 seconds.
- Programme self-purpose and approved-presence tests passed 24 cases in 91.81
  seconds; private wayfinding/modular composition passed 17 in 118.70 seconds.
- A real same-person accepted-work/hosting test passed independently, followed
  by both native full-profile and Workforce-only export assertions.
- The later private HTTP/composition run passed 24 PostgreSQL cases in 201.69
  seconds: real signed-in owner, another person, all four formats, native
  withdrawal, exact scope and missing/changed owner evidence.
- The latest focused catalog/public/private format/page run passed 172
  database-free cases in 1.46 seconds. Strict typing and semantic docstring
  checks passed for the private page, compositor, formats and Events label.
- Earlier development failures included a wrong envelope re-export, optional
  typing and assertions expecting the old audience-specific error wording;
  these were repaired before the corresponding passing reruns.

The consolidated PostgreSQL owner/output/host-copy/native-manifest set passed
129 cases in 721.59 seconds without failures. Repository-wide mypy passed 549
source files; lint/formatting, strict docstrings and semantic documentation
validation (566 source files) passed. Propagated owner exceptions are documented
in Notes separately from directly raised errors, satisfying the strict checker
without changing behavior or waiving its rules.

These overlapping development counts are not additive whole-suite certification.
Exact-commit local/hosted acceptance remains pending at this snapshot; CURRENT
and the eventual PR own newer evidence.

## Synthetic browser observations

Both components used isolated loopback fixtures and synthetic data, not an
activated Programme profile. Public rehearsal covered seven widths, keyboard
filtering/disclosure, complete copies and print-friendly HTML, denial/recovery,
native exact-copy invalidation and release withdrawal. It corrected excessive
above-content provenance. An initial fixture expired after its lease and is
explicitly incomplete; the fresh fixture closed normally in 146.11 seconds.

Private rehearsal covered seven widths (320, 390, 768, 958, 1,024, 1,280, 1,920),
one H1/main, keyboard disclosure, both download-link actions, print-friendly HTML,
another-person empty state, source failure/recovery, native withdrawal and
sign-in protection. Page scroll width did not exceed the viewport. It found pale
agenda headings inherited from the shared theme; explicit inherited text color
corrected the visible defect. Browser warning/error logs were empty. Server logs
retain the expected injected 503 and fixture favicon 404. The fixture closed
normally in 256.29 seconds and the viewport override was reset.

Real 200% browser zoom, native print output, representative screen-reader and
on-site comprehension remain unproven under #92. These checks are deferred, not
waived. The post-rehearsal row-source print disclosures retain stable private
identities and are covered by template tests, not a claimed second human pass.

## Recovery, decomposition and next delivery

No model/migration, runtime writer, current profile pin, production URL include,
notification handler or deployment is added. Existing native guards, schema
fingerprints and immutable release evidence remain unchanged. Fix-forward keeps
owner evidence intact; ordinary saved files cannot be remotely recalled. #97
logical restore remains mandatory before activation or a director pilot.

Native child #100 explicitly retains the original room/department/operator
acceptance and follows #99's protected merge. Its protected delivery/staffing
layers are not waived or labelled implemented by this component. Detailed change
impact, delivery/acknowledgement, on-site continuity, guided setup and integrated
acceptance remain #48 work. No production data or unrelated Docker cleanup is
authorized. The maintained handoff is shortened to current facts; older evidence
remains in existing append-only checkpoints and Git history.
