# Programme accepted-conversion implementation checkpoint

Date: 2026-09-06

## Outcome and scope

Issue [#77](https://github.com/martonpornoi/maru/issues/77), a native child of
[#48](https://github.com/martonpornoi/maru/issues/48), implements the explicit
accepted-proposal-to-private-item boundary under PRG-008 and ADR 0086.
Applications retains one exact immutable accepted transition and orchestrates
Programme's public source-bound creation command. Reciprocal references,
independent authority, current accepted-source checks, shared retry identity,
seven initially required readiness concerns, and both owners' success evidence
commit atomically.

This checkpoint is implementation evidence, not a claim of hosted acceptance.
The linked issue and its protected pull request retain exact candidate and
merge evidence. Continue single-agent through that one issue's verification,
protected merge and main synchronization; do not start host relationships in
the same task.

## Verification at this checkpoint

- 449 Programme-focused unit tests passed.
- The initial conversion service and separate-connection race group passed
  25 tests; the expanded race/recovery group passed 17 tests. These groups
  overlap and must not be summed as a unique test count.
- Withdrawal, reopening, recusal and Department retirement were exercised in
  both conversion orders. Duplicate intents either replay or conflict without
  a second target.
- Empty schema reversal/reinstall, populated downgrade refusal and guard
  execution-privilege drift passed against PostgreSQL 17.11.
- Both owners' actual schema and integrity readiness checks passed after
  fingerprint finalization. Strict mypy passed for 58 affected owner sources.
- Related regressions, final documentation checks, exact clean-commit local
  certification and independent hosted acceptance remain delivery gates.

One loopback-only temporary container, `maru-issue77-focused`, owns focused
synthetic test data. Existing Docker resources, both retained user stashes,
and the separate main worktree are not cleanup targets. Remove only this
task's disposable container after focused verification finishes.

## Maintained contracts and remaining work

See [conversion ownership](../modules/programme-conversion.md) and
[migration/recovery](../operations/programme-conversion-migration-and-recovery.md).
The new source remains runtime SELECT-only. Existing manifests, generic target
fences, routes and delivery activation are unchanged. No host, co-host,
Scheduling, venue booking, Shift, account, Registration, Participation, payment,
attendance, or public timetable is created.

Programme-owned host relationships are the next separately authorized child.
Scheduling, staffing, timetable release, continuity, setup and the integrated
department journey follow in dependency order. The umbrella's integrated
acceptance remains incomplete; this foundation alone is not a usable Programme
department workspace or production release.
