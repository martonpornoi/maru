# Programme continuity surfaces and bounded browser evidence

Date: 2026-09-13
Issue: #107 under #48
State: Local implementation milestone, not exact-head certification or delivery

## Outcome and contracts

On branch `codex/programme-onsite-continuity`, following source/offline commit
`99d6e73`, Scheduling adds the three dormant public, My Maru and Administration
live/print/signed-pack surfaces. Shared templates retain one H1/main and the
existing shells. Exact authenticated private actors, independently admitted source
queries, closed safe inputs, no-store responses, source/expiry warnings, explicit
operator layers and #104 notice continuations remain intact. Signing absence
disables issuance, not independently authorized live viewing. No production route,
profile, schema, key, permission or worker was activated.

The new operator runbook documents canonical process signing policy, separately
authenticated public trust, exact expected scope, connected initialization,
protected high-water/history, actual-clock verification, new-file publication,
lock/clock/storage failure, rotation, disposal and stop-use. It explicitly notes
that cleanup failure can follow complete output publication: a nonzero command
does not authorize using any resulting file. Saved HTML is dated output, never
a live verifier; existing copies cannot receive unseen changes or remote erasure.

## Automated component evidence

- HTTP tests use real shared templates and real Ed25519 signing with synthetic
  owner projections; they cover exact actor/scope/layers, safe methods, parsing,
  denial/unavailability, missing configuration, dormant routes and stored markup.
- The latest focused payload/HTTP run passed **90 tests in 3.72 seconds**, including
  three audience-specific unavailable-to-fresh context recovery cases.
- Earlier separate component evidence remains the 5,729-unit/45.30-second run,
  68 presentation/state cases and 15 real temporary-file cases in the preceding
  source/offline checkpoint. They are not combined exact-head certification.
- A first targeted command named a nonexistent presentation test module and ran
  no tests; corrected selection uses the payload module that owns those cases.
- Ruff identified an unused mock-lambda argument after the recovery test; it was
  renamed to an explicitly unused argument before final checks.

Three maintained native cases were added, **not run** under ADR 0100: public
release withdrawal/no side effects and adapter denial; exact-person Workforce-only
retained work with excluded-module SQL checks; operator full-envelope purpose,
private-layer denial and fail-closed owner audit persistence. #102 owns execution.

## Synthetic browser rehearsal

An isolated loopback foreground fixture used only artificial immutable DTOs,
ephemeral signing material and mocked owner admission. Database connection entry
was patched to raise, not routed to any database. No real people or keys were used.
It rendered actual application views/templates/assets. The in-app browser viewport
was **1280 by 720 CSS pixels**; page width remained within the viewport and each
observed application state retained one H1 and one main landmark.

Observed public and own-person available, empty, withdrawn, invalidated, denied
and unavailable states; public long-title next entry; personal hosting unadopted
with retained work; and all six operator states. Withdrawal/invalidation hid
ordinary Programme geometry without erasing the own work card. Denials contained
no normal agenda. The base operator view omitted private technical material;
selecting Technical instructions displayed it, and selecting Technical plus
Linked staffing displayed both the event and linked work card. Selected controls,
source ceilings and unique IDs were retained. The print-friendly operator view
retained selected technical instructions and printing guidance, removed download
controls and kept the single-shell landmarks without horizontal overflow.

Visual inspection found the original source explanation pushed Now too far down.
The header was shortened, detailed offline instructions moved into a collapsed
footer disclosure, and source/expiry warnings preserved. Repeat inspection placed
Now and Next in the initial desktop view. A subsequent audience-state sequence
exposed the fixture's reused mock admin-context dictionary, not a native owner
failure: stale status text appeared in a 200 response. Fresh per-request mock
context fixed it; repeating unavailable-to-available recovered the actual agenda.
The corresponding unit fixture and three recovery regressions now reflect the
real admin context's per-request behavior.

## Explicit limits and next step

This was a component browser rehearsal, not proof of real grants, database locks,
audit persistence or the complete notice workflow. Notice destinations deliberately
stop at the fixture boundary. No download-dialog custody, native printing, automated
accessibility scan, screen-reader path, keyboard/focus traversal, genuine zoom,
reduced-motion control or remaining 320/390/768/958/1024/1920 viewport was accepted
in this run. Record those exact remaining browser/human checks under #92 before
integrated acceptance or activation; do not relabel the 1280 check as the full
UX-029 matrix. Static HTML contains no animation or offline JavaScript channel.

Next run final static and exact-commit deferred development acceptance, submit
the protected PR, then require its exact-head PR gate and CodeQL before squash.
#107 remains open until protected delivery. #108's connected departmental workflow
follows; #102/#97/#92/#109 gates remain open. Preserve unrelated worktrees, stashes
and containers; retire only this exact disposable preview after rehearsal.
