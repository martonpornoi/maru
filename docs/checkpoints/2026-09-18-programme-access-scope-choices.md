# Dormant labelled Programme access scopes

Date: 2026-09-18. Part of #108's existing accountable-setup/navigation item under
#48, not completion or activation of either issue.

## Outcome and boundary

Authorization owns a complete bounded chooser for current ordinary controller
scopes: shared Organization Venue, Edition, Department and canonical selected room.
It reuses Workforce's public current-Department identifiers and its own typed room
bindings, independently proving each scope before reading its label. Limits are
256 Departments and 256 room candidates plus the two broader scopes. Incomplete
sources fail without a partial list or hidden count; no admitted scope is neutral
denial. Narrow controllers need no personnel-directory or sibling authority.

Foundation/person locks, exact Programme profile, native integrity, repeated source
comparison and security-extended sensitive-read audit precede disclosure. Metadata-
only entry admission reads no names and adds no activity audit. Strict GET-only HTML
uses the shared shell, checks route name/callable/full scope and revalidates after
rendering. Existing request/review screens receive a validated return link.

This implements IDN-002/004/014, EVT-006/007, AUD-001, UX-005 through UX-008,
UX-019/020/027/029 and NFR-013 under unchanged ADRs 0081, 0100 and 0106. It creates
no request/grant/invitation, introduces no migration/runtime privilege/profile or
production URL, and preserves all excluded-product boundaries. Removing an unused
reserved UI does not delete retained authority evidence. Complete native performance,
race/isolation and runtime/recovery acceptance are not established by bounded scans
or stubbed tests.

## Verification performed

Final complete database-free unit feedback: 9,782 passed in 57.18 seconds, including
69 new query/view cases and explicit platform-principal preflight denial.
Existing three URLField deprecation warnings remain unrelated.
Strict mypy passed 717 source files; Ruff and strict public NumPy/semantic docstrings
passed. Final exact-commit certification and protected hosted acceptance are separate
pending delivery gates, not implied by feedback.

Tests exercise exact predicates and 256/257 boundaries, duplicate/malformed/foreign
owner references, narrow-controller labels, no-admission denial, inactive people,
integrity/audit/source failure, metadata-only discovery, strict transport, escaped
labels, complete exact link identity, response bounds and post-render withdrawal.

Database-prohibited loopback browser fixture used actual templates/views with
explicitly stubbed owner projections. It observed all four labelled scope cards,
broader Venue consequences, Tab-visible focus and Enter to Department request
creation, exact-room review/empty inventory and return links. Narrow-only, denied,
unavailable, overflow and changed-source responses were inspected; private labels
were withheld in the latter failure states. All seven contract widths
(320/390/768/958/1024/1280/1920) had no page overflow and one H1/main.
The initial 17-pixel link targets were corrected and reobserved at 44 pixels at every
width; the 390-pixel card layout was visually inspected. Warning/error logs were
empty. Only exact fixture processes/tabs were stopped; viewport was restored.

Three native cases were maintained in `test_programme_role_schema.py`: audited
no-grant labelled discovery, foreign-organization denial, and real scoped-controller
grant/revocation with metadata-only admission and no discovery activity audit.
They remain **uncollected and unexecuted** under #102. Actual 200% zoom, screen-reader
comprehension, native/two-human independent decisions and complete workflow acceptance
remain #92/#109. This component introduces no motion, modal or mutation; it does not
re-certify the existing request editor's native discard behavior.

## Remaining sequence

Finish this increment's protected delivery, connect fixed authorized Programme entries
through the shared registry, and prepare the complete isolated synthetic fixture.
The twelve-checkpoint integrated protocol is not a launched or accepted full fixture.
Restore #102 native acceptance and resolve #97 logical recovery, then collect #92/#109
human/integrated evidence before separately verified promotion. #108/#48 remain open.
