# Dormant Programme operational-role definitions and intent contracts

Date: 2026-09-17
Issue: #108, within #48; native/human/integrated gates remain open.

## Outcome and boundary

ADR 0106 records genuine independent Programme operational approval without
widening either representation root. Authorization owns 27 immutable optional v1
role recipes, explicit capability/scope consequences and complete definition
digests. Events' independent catalog snapshot registers their exact owner entries;
both current profile manifests remain byte-for-byte unchanged and admit none.

Closed immutable scope/intent inputs reject unknown/coerced definitions, malformed
identities, self approval, inapplicable scope facts, naive timestamps, empty
intervals and unbounded/control-bearing rationale. Canonical digests bind original
normalized facts and exact recipe contents. Terminal-action digests distinguish
approve, decline and cancel; computing one takes no action.

This is a database-free prerequisite, not grant implementation or end-to-end setup.
No schema, runtime role, route, task, domain event, audit or source capability was
added or changed. Current authority checks and immutable representation roots
remain unchanged. The actual request/decision schema, locked owning commands,
genuine-person approval surface and integrated fixture remain unfinished.

## Verification and next gates

Focused database-free recipe/input/adoption tests passed: 117 cases in 0.47s.
They pin all v1 definition contents, check every allowed/rejected scope, preserve
profile exclusion, reject malformed intent and bind each retry-relevant fact.
Complete unit feedback passed 9,297 cases in 55.41s; strict types passed for 698
source files, semantic docstrings for 719 and documentation validation for 594
Markdown files. A later malformed-resource-kind regression was added after that
iteration. Exact clean-commit development certification must pass before protected
delivery; the delivery record will retain actual results.

PostgreSQL suites remain uncollected/unexecuted under ADR 0100. No native case is
added by these pure contracts. Future schema integrity, real controller eligibility,
horizon/revocation, actual approval, concurrent terminal decisions, rollback,
runtime role, recovery and excluded effects require maintained #102 cases. #97,
#92 and #109 remain mandatory final gates. Approval for the previous setup schema
observation is spent and is not permission for the new Authorization tables.
