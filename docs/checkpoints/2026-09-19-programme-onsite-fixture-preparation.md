# Programme P09 HTTPS and print-response fixture preparation

Date: 2026-09-19. Scope: #108/#48, following protected PR #186.
Contracts: SCH-006/008/010/012, OPS-009, NFR-001/005/013 and existing ADRs
0081, 0097, 0099, 0100 and 0103. No schema, migration, role,
profile, dependency, signing key or CI policy changes. One dormant presentation
label is corrected alongside the maintained fixture.

## Prepared observation

The guarded host-only journey continues its actual P08 release through anonymous,
genuine host, volunteer, exact first-room operator and planner Department/edition
HTTP requests. Each uses real candidate routes and the existing owner projections.
No Django test client, forced session, browser imitation or fabricated owner
permission enters the maintained native case.

The transport checks the existing fixture's exact loopback HTTPS origin, port and
leaf fingerprint before loading only that certificate as trust. It ignores proxies,
never follows redirects, rejects foreign/malformed paths, restricts POST to login
and logout, bounds reads to 2 MiB and fifteen-second socket waits, and retains the
original finite lease. The actual login form supplies the CSRF token; the POST uses
same-origin headers and requires the exact intended redirect plus a secure HttpOnly
session cookie. Own logout uses the rotated CSRF cookie, checks its result and
forgets all in-memory cookies. Response bodies and headers are excluded from repr;
fixed errors do not print private bodies, keys, paths or credentials.

Six exact purposes each request fresh timetable HTML, print, JSON and calendar plus
now/next HTML and print. Assertions retain release two, occurrence membership,
edition time zone, one main/H1, no-store/nosniff and correct content types. Now/next
print retains source check, replacement/expiry warning, historical-copy limits and
no attendance inference; it omits signed-download controls. The current clock is
not falsified to make the future fictional event appear to be happening now.

Personal JSON must identify the actual signed-in person. Hosting contains only the
ceremony occurrence; volunteer work contains three exact current commitments and
the separate removed/cancelled predecessor, without unrelated hosting discovery.
The first-room operator sees exactly its two occurrences without optional fields;
technical-field and edition escalation are refused. Planner Department/edition
exports explicitly request staffing, never delivery fields. Repeated parameters,
extra actor selectors and anonymous personal access are refused. Missing dedicated
signing configuration returns unavailable, not an unsigned pack. Private discussion,
passwords and other people’s emails are absent; the authenticated shared shell may
legitimately identify its own viewer. No output action sends or acknowledges notices.

## Verification and remaining work

P09 source review found that every historical staffing link said "not cancelled,"
including links to an actually cancelled demand. The dormant continuity source now
says "retained predecessor; see demand status" and keeps the independently reported
status unchanged. Regression cases cover open, locked and cancelled history, plus
the existing shared HTML/print surfaces. This bounded on-site consistency fix is
part of #108, not a new lifecycle or authority rule. CHANGELOG records it.

Continuity checks compare complete source-row membership, not just a release
heading. Requested operator staffing must retain the exact current demand versions
and separate cancelled predecessor. No historical row is relabelled as current.

Focused database-free tests passed 222 cases in 6.30s, including 79 new helper
cases and the affected owner rendering/shared-view regressions. They cover the genuine certificate parser
and verification configuration, simulated HTTP/CSRF/cookies, bounded failures,
format/warning checks and deliberate actor/source/room/work disclosure regressions.
These prove helper behavior, not a successful TLS handshake or native database
journey. The host-only case is maintained but uncollected/unexecuted under ADR 0100.
Complete feedback passed 11,408 units in 61.83s with three existing Django URL-field
warnings. Ruff formatting/lint, whitespace and documentation validation passed
(639 Markdown files, four repository skills and 215 requirements). Exact
certification and protected delivery are pending.

P10 signed/offline continuity, P11 restore/archive/stop-use and P12 complete excluded
effect/isolation inventories remain next. Restore PostgreSQL through #102 only after
checkpoint preparation. Native HTTP performance and owner authorization remain
unmeasured. Browser layout/keyboard, genuine zoom, native print/pagination, screen
reader and representative-person checks remain #92; this code proves none of them.
#97/#109, #108 and #48 remain open. No activation or pilot approval is implied.

## Protected delivery evidence

PR #187 squash-merged at 2026-09-19 15:17:33 UTC as
`8450f80078629e482744b2af34037d8298dff359`. Its tree
`604fe23ec29515f8147a3a7608d9b99f3d8a7531` equals certified head
`6039cb2e8f5da2c0baa52574c77eef3cd38c25af`. Clean local main was fast-forwarded
to that exact origin/main result; the separate repair worktree stayed unchanged.
Complete reviews, conversations and closing-reference collections were empty.
Exact-head PR gate and all three processed CodeQL configurations passed, with no
findings/errors/warnings. Hosted run 35450706624 took 9m48s for quality, 2m34s for
units and three seconds for the aggregate gate.

Exact local certification completed at 2026-09-19 15:04:19.7603839 UTC in 342.245s
(5m42s). All eight retained gates passed, including 11,408 units in 63.38s and 103
frontend cases. Its v4 receipt explicitly reports PostgreSQL deferred, zero database
instances and null combined coverage/timing headroom. Receipt SHA256:
`9d11569f2d14a91ab36853ef25bac4292af08495b9050d78e267535b5185d8c9`.
Five artifacts are archived and hash-matched in the ignored local
`.tools/certification-evidence/programme-onsite-6039cb2-deferred/` directory.
An initial archive command guessed the wrong package directory; it did not copy
the two packages. They were subsequently copied from the verified
`.local-ci/package-dist/` location and all five hashes independently checked.
This archive repair changed no source or certification result. #108/#102 retain
linked merge/debt comments; no parent or acceptance gate was closed.
