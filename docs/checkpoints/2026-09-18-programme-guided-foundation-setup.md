# Dormant guided Programme foundation setup

## Outcome and boundary

This increment of #108/#48 provides labelled new/existing foundation selection,
explicit original-intent creation, an exact original-actor receipt and truthful
handoff to existing Representation & access. Organizations owns bounded minimized
choice references; Events owns the platform-admitted projections and shared-shell
adapter over the delivered atomic command. No schema, migration, runtime privilege,
current profile or production route changes. No invitation, personal acceptance,
activation or operational authority is inferred or performed by the setup page.

The route pins mode and reused scope. Original source fingerprint/key survive
errors; current source changes are explained without replacing original intent.
Receipts report which levels were created versus reused, independently of today's
representation state. Full original actor/scope predicates precede identifying
owner reads, and audit/current Identity/final render comparison precede disclosure.
Inventory overflow never exposes a partial list or cross-tenant series selection.

## Automated implementation feedback

- Initial focused stack: 300 passing tests in 2.14s.
- Final 92 new query/form/view cases passed in 1.60s after original-mode receipt
  clarification; the complete database-free suite passed **9,601 tests in 55.61s**.
- Strict mypy passed all 711 source files. Ruff and strict NumPy documentation
  passed; semantic docstrings validated 732 source files.
- A nil-key test initially failed: the form now rejects nil before the command.
  The raw-duplicate fixture initially collapsed QueryDict values during encoding;
  real URL-encoded repeated values now prove pre-query rejection. No database
  access or skips were enabled. Documentation parser failures were corrected by
  explicit individual parameters and concrete public error documentation.
- Three native setup projection/isolation cases are maintained in the existing
  schema suite but remain uncollected/unexecuted under ADR 0100 and #102. They
  cover actual original receipt/choices/audit, another platform actor and foreign
  series reuse. No database timing, combined coverage or runtime proof is claimed.

## Synthetic browser evidence and limits

The loopback-only fixture had a 20-minute lease, synthetic accounts, stubbed owner
reads/writes/Identity and a hard database-connection prohibition. Real templates,
forms, CSRF, shared-shell CSS and pending-input script were exercised.

All three setup modes reached simulated original receipts through ordinary labelled
links/forms. Keyboard Tab/Space/Enter completed reused-series confirmation, with
visible solid focus and the checkbox kept in view. Conflict retained visible dates,
edition and reason, focused the alert and did not resubmit automatically. Changing
the fixture source through its separate control page showed the explicit original-
snapshot warning. Hidden original key/fingerprint retention is automated-test
evidence, not inferred from the browser's restricted hidden-value readback.

Empty, denied, unavailable and overflow states were observed without private names
in failures. Provisioning and active-root receipts clearly separate responsibility
from supplemental operational approval. Reviewing the receipt prompted explicit
original creation/reuse wording; the corrected existing-organization/new-series
result was then observed. The fixture's initially fixed parent labels were aligned
with submitted synthetic names; this was fixture correction, not a real command.

At 320, 390, 768, 958, 1024, 1280 and 1920 CSS pixels, the form had no horizontal
page overflow and one H1/main; observed controls were labelled. Submit targets were
44 pixels. Visual review aligned submit/date styling with shared theme variables and
enlarged the actual confirmation label to at least 44 pixels (84 at 320). Final
responsive checks passed and browser warning/error logs were empty. Task-owned tabs,
temporary viewport and server were cleaned up; no Docker resources were started.

This is not actual database setup, genuine-person representation activation,
screen-reader comprehension, native Cancel/discard, actual 200% zoom, reduced-motion
acceptance, or a complete integrated journey. The representation destination was
identified but its genuine-person workflow was not rehearsed in this component.
Those checks remain #92/#109 with #102/#97 prerequisites. Operational request
creation, shared final navigation, the full isolated fixture and final promotion
remain unfinished. Exact-commit certification and protected delivery are subsequent
gates; #108/#48 remain open.
