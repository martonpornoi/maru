# Exact personal Programme decision receipts

Date: 2026-09-14. Scope: a dormant #108 review/decision increment under #48.
Base: protected PR #119, `c6f26d1033a37d780c1d05341a25049b81814c2b`.

## Human and owner contract

The [page contract](../product/page-contracts/programme-decision-receipts.md)
implements PRG-004 and ADR 0085 through personal chronological history, exact
escaped historical messages and independently authorized explicit own receipt.
An exact getter uses the same scoped recipient projection as history, without
scanning arbitrary pages. It returns no private review evidence or another
person's receipt. Inclusion belongs to the reviewed immutable seal, not today's
collaborator roster or call/Department/planning lifecycle.

The existing closed review command remains the only writer. It receives the
authenticated actor, exact organization/edition, no Department override, exact
case/decision IDs, original case version/retry key and blank personal reason.
Receipt is not agreement, hosting or publication consent. Exact retries reach
the retained owner replay boundary; conflicts preserve original proof and show
newly authorized historical state without silently rebasing a command.

The shared shell has one H1/main, labelled plain controls, escaped wrapping
messages, focused errors and the existing pending-input guard. The exact dormant
template's shared asset markers are registered, with explicit regression coverage
that other Programme markers and production routes remain forbidden. No generic
API, profile, production URL, database schema or new writer is activated.

## Local evidence and earlier failures

- Initial focused HTTP/query feedback passed 62 cases in 1.40s.
- A full unit run exposed the missing dormant-template marker registration and
  was stopped after subsequent Windows temporary-directory errors. Fail-fast
  reproductions isolated these separately. The exact template entry and an
  adversarial marker assertion were added; no broad containment exemption.
- The environment error was `PermissionError` on the pre-existing Windows
  `pytest-of-TheMw` directory. A fresh task-owned temporary root produced a
  complete passing run: 6,638 units in 42.22s, with three existing URLField
  warnings. The inaccessible old directory was neither deleted nor permission-
  modified. Focused checks disabled the unrelated inaccessible pytest cache.
- The final additional receipt/containment tests passed in the 85-case focused
  group in 1.73s. Tests cover scoped exact reads and audit failure, field ceilings,
  missing/foreign recipient/cursor, independent read/write, retained lifecycle,
  strict versions/retry/confirmation, duplicates/files/unknown fields, no GET
  mutation, CSRF/method guards, exact writer signature, replay/stale proof,
  outcome labels, late-render revocation, escaping and dormant routing.
- Full focused typing and NumPy documentation pass. A test originally assumed
  a nil-UUID restriction absent from the canonical owner retry contract; that
  expectation was corrected, not added as an unrelated behavior change.

## Synthetic browser evidence

The in-app browser used the actual views/templates/shared assets with synthetic
authority/query/command stubs and a fixture-only CSRF bypass. Every database
connection was explicitly forbidden. At 1280 CSS pixels the document width was
1280, with one H1 and one main landmark. Ordinary history links opened the exact
message; a deliberate checkbox/submit changed the fixture's own receipt state.
Script-shaped message text rendered as text. A stale submission retained the
checked confirmation, displayed the pending status and focused the visible alert;
the screenshot was inspected. Original hidden version/retry values are evidenced
by HTTP tests, not claimed as browser-observed. Read-only content, truthful empty
history and generic denied state were also observed. Both owned tabs and the
loopback server were closed after use.

This is neither persistence, native authority, full responsive/keyboard/zoom/
screen-reader acceptance nor integrated Programme proof. Those remaining human
checks belong to #92. Four existing native review lifecycle/query scenarios were
extended with exact getter checks, own-field filtering, outsider denial,
removed-collaborator and retired-Department retained history. They were maintained
but not collected or executed; #102 owns restoration and acceptance.

## Delivery and continuation

Exact clean-candidate certification and protected hosted delivery are pending
at this checkpoint. PostgreSQL remains deferred under ADR 0100; no combined
coverage or timing-headroom claim is made. A later protected checkpoint will
record the exact committed result rather than rewriting this historical stage.

Continue #108 with independent organizer review-policy/case/assignment tasks,
reviewing/moderation/accountable decisions and accepted conversion, then remaining
reference/file choosers, owner connections, setup and isolated integrated fixture.
#108 and #48 remain open. #102, #97, #92 and #109 still precede final promotion;
no production data, deployment, unrelated capability or broad cleanup is authorized.
