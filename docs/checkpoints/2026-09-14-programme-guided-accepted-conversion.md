# Guided exact accepted-Programme conversion

Date: 2026-09-14. Owners: Applications orchestration and Programme creation/read.
Scope: bounded #108/#48 continuation from protected PR #126; no activation.

## Outcome and boundaries

PRG-008, QRY-005–008, AUD-001 and UX-029 under ADR 0086 govern the
[conversion page contract](../product/page-contracts/programme-accepted-conversion.md).
The dormant review root gains labelled historical-acceptance discovery, exact
decision/seal selection, inspected effective eligibility/consumption and explicit
private copy/reason/confirmation. New Applications-owned queries require current
conversion and independent Programme management authority before scoped storage,
locking and audit. They expose labels and versions, never review messages,
answers, profiles, people or private item titles. Programme's separate audited
creation query exposes only its control cursor/planning hint under manage authority,
without requiring a private inventory read. Absent used control is unavailable.

The canonical conversion writer, its transactional reciprocal evidence, capacity,
source, version and retry rules are unchanged. Confirmed original POST reaches
that owner before fresh source/cursor reads. Identity/tenant/both adapter pins
still guard replay, but fresh Department, write or source admission is not added
as an accidental original-receipt prerequisite. The optional item continuation
uses its independently authorized private read after receipt recovery; revocation
removes the link while preserving a still-admitted minimal creation receipt.
Decision overview offers conversion navigation only after independent admission
and rechecks it before disclosure. No source content is copied automatically.

No schema, migration, runtime grant, dependency, production route, profile, new
activity collection or CI-policy changes. Historical receipts prove original
creation only, never current item state, hosts, publication, placement or staffing.
All initial readiness concerns remain required and unsatisfied. Existing failure
and rollback fences are unchanged; no deletion or compensating rewrite is added.

## Iteration evidence

- Complete inexpensive preflight: 7,229 database-free unit tests passed in 53.71s,
  with three existing URLField warnings. No PostgreSQL collection/execution.
- Final focused query/form/HTTP/decider-navigation/containment feedback: 118 passed
  in 2.15s after extracting the small navigation helper to preserve the existing
  complexity limit. Full Ruff lint and formatting passed across 1,216 files.
- Focused typing, NumPy and semantic Python documentation passed before that
  behavior-preserving helper extraction; clean-head certification rechecks all.
- Tests exercise both owners, complete bounded scoping, audit failure/revocation,
  no private copy, creation without private inventory, deliberate closed forms,
  real CSRF, both original cursors/retry preservation, stale/consumed/read-only
  states and original receipts before fresh reads. Optional continuation denial,
  unavailability and revocation never grant item content or lose admitted receipts.
- One real-owner source/cursor/form/commit/replay scenario extends the existing
  native conversion file, with source consumption and wrong-Department denial.
  It remains uncollected/unexecuted ADR 0100/#102 debt. No new weighted test file,
  invented timing or combined coverage claim.
- Initial feedback corrected a test's missing reserved-route prefix, explicit
  dormant template markers and navigation typing/complexity. No canonical writer
  or acceptance threshold changed to make tests pass.
- Exact clean-commit certification and exact-head hosted PR gate/CodeQL remain
  required after this checkpoint; this is not a certification or merge receipt.

## Bounded browser evidence and remaining gates

Loopback synthetic fixture with actual Django shell/forms and shared pending-input
assets; mocked owner authorization/query/command persistence, all database
connections forbidden and fixture-only CSRF bypass. Ordinary labelled controls
observed historical-source discovery, blank deliberate title/summary/reason and
unchecked confirmation. After entered input, a stale command preserved all
visible text, both original versions and checked confirmation; the alert received
focus. Exact replay showed original creation versions and a minimal receipt with
no independently admitted item link. Read-only, ineligible and consumed sources
offered no fresh form, and denied detail returned generic text. At 1280 CSS pixels,
document client/scroll width was 1265, with one H1/main; screenshot reviewed.
Task-owned tabs were closed and the synthetic server stopped afterward.

No native persistence, successful real cross-module transaction, message delivery,
complete keyboard/all-width, genuine 200% zoom, reduced-motion, screen-reader or
human acceptance is claimed by that rehearsal. #92 owns genuine human acceptance;
#102 native/full-coverage restoration, #97 logical recovery and #109 integrated
acceptance remain required before #108 promotion or #48 closure. Next work stays
inside the recorded #108 connections, host retry recovery, structured viewers and
accountable setup; #113 remains recorded infrastructure, not a detour absent an
actual protected-delivery blocker.
