# Dormant exact Programme review-case intake

Date: 2026-09-14. Branch: `codex/programme-review-cases`, based on protected
PR #121 (`848f1246a8663e84a8799e5e7b4edbd695ff7efc`). This is a bounded #108
increment within #48, not complete review, activation or production readiness.

## Outcome and contracts

Applications connects saved immutable policies to a complete bounded chooser of
currently eligible submitted seals and deliberate exact-source case opening.
Call labels, sequence, sealing timestamp and disambiguating references replace
UUID discovery; private answers and people are not inferred as labels.

Both seal and proposal are organization/edition scoped through the exact current
owner Department and call. Source/current-pointer, lead, every retained
collaborator and existing-case exclusions happen before pagination, using the
existing manager's exact `review_setup` field. Audit precedes disclosure under
the existing edition consistency boundary. No case roster or private evidence
is added to setup authority.

The exact selection remains readable after eligibility changes. Confirmation
uses the existing canonical writer with route-bound seal/policy, canonical
creation version zero, reason, confirmation and original retry proof. Original
POSTs reach canonical replay after case creation, source changes or closed
planning; no adapter preflight silently substitutes a newer seal or blocks a
retained receipt. Conflicts and recoverable service errors retain bound controls
when current protected reads succeed; failed reads/audits return generic errors
instead of stale private content. Successful receipt contains only owner-issued
case/version/receipt references, not new authority.

PRG-003 and ADR 0085 remain authoritative. No new ADR, model, migration, runtime
privilege, dependency, production route, profile or PostgreSQL execution policy.
The [page contract](../product/page-contracts/programme-review-setup.md) and owning
module/API documentation describe the new reserved task. Generic fences remain
closed. Rollback can remove dormant adapters without altering retained cases;
existing immutable owner receipts and recovery rules remain mandatory.

## Verification and honest gaps

- Focused database-free acceptance: 53 passing intake query/form/HTTP tests,
  including exact scope, pre-pagination filters, canonical proof, unchanged
  retry after lifecycle changes, validation, duplicate/unknown/file input,
  CSRF, safe errors, read-only states and before/after-render revocation.
- Complete Ruff and formatting checks pass (1,187 Python files); focused strict
  typing passes for both new source modules. Documentation validation passes
  513 Markdown files/four repository skills/215 stable requirement IDs; semantic
  Python documentation passes 639 sources.
- First full unit preflight: 6,787 passed and one timing-inventory mismatch for
  the new native file. Registered its file/current-schema group with a provisional
  300-second scheduling weight. This is not a measurement or guaranteed upper
  bound; #102 must calibrate it. Existing measured weights were not reduced.
- Early focused tests corrected an imported fixture registration, HTML textarea
  newline expectation and production admin catch-all/header assumptions. The
  source behavior was not weakened to satisfy these tests.
- Native fixture inspection caught a pending invitation preventing submission;
  it now explicitly declines the invitation and verifies retained-conflict
  exclusion. The maintained native scenario remains unexecuted #102 debt.
- Synthetic real-browser rehearsal followed policy → labelled source → reason
  and confirmation → owner receipt. The screenshot inspected at 1280 CSS pixels
  shows one H1/main, no document overflow and escaped call text. Read-only source
  selection and generic exact-source denial were observed. All database
  connections were forbidden; authorization/query/command behavior was stubbed,
  with fixture-only CSRF bypass. Task-owned tabs and server were stopped.
- Stale/replay recovery is HTTP-test evidence, not an exercised browser claim.
  Full widths, native zoom, keyboard, screen reader, reduced motion, independent
  real roles and integrated recovery remain unchecked #92 acceptance.

Final inexpensive preflight passes all 6,788 units in 40.78s, with three existing
URLField warnings. Focused NumPy documentation also passes. Exact clean-commit
certification and protected hosted delivery are pending at this implementation checkpoint. No earlier
receipt certifies this candidate. PostgreSQL was neither collected nor executed.

## Next

Deliver named assignment management, then independently authorized reviewer,
moderator, decision and accepted-item conversion tasks. Keep the remaining
personal reference/file choices, readable retained decision-source connections,
labelled timetable/staffing/release/notice/continuity links and accountable setup
explicitly open in #108. #109 integrated acceptance, #102 restoration/exhaustive
native checks, #97 recovery and #92 genuine human evidence precede separately
verified final profile promotion. #48 remains the first-priority umbrella.
