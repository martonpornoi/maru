# Programme own-person approval workspace

## Outcome and limits

This #108/#48 increment implements the reserved genuine-person review screen over
PR #158's commands. Authorization owns the complete bounded own-request query,
actual-person forms and decisions. The shared shell exposes exact scope, recipe
consequences, verified display labels, original interval/reason and historical
terminal outcomes. It adds no contact directory, schema, migration, runtime
privilege, current-profile capability or production route.

Current ordinary controller authority, complete target scope and retained intent
integrity precede disclosure. Recipient status or platform fallback alone is not
access. Every sensitive result, including an empty inventory, is audited. Final
rendered source/authority changes suppress output. Strict POST, real CSRF, deliberate
confirmation and original retry identity bind decisions. Rendering cannot regrant
an output or repeat a successful command. Request creation and foundation guidance
remain the next continuation; the complete isolated fixture is not delivered here.

## Implementation feedback

Focused database-free feedback passed 183 approval-stack tests before the final
terminal-author fixture regression. Types, strict NumPy documentation, semantic
documentation and Markdown validation passed. The first HTML run correctly hit the
database prohibition through an unstubbed shared-shell helper; the isolated fixture
was corrected without enabling database access or skipping cases. HTML assertion
corrections account for Django's stronger no-cache headers and textarea newline.
The initial mistyped semantic-validator command did not run; the maintained
`validate_python_docstrings.py src scripts` invocation subsequently passed.

Five maintained native reader cases cover each actual author/approver, an unrelated
controller who is merely the recipient, known foreign scope and historical reading
after output revocation. They remain uncollected/unexecuted under ADR 0100 and #102.
No native timing, combined coverage, transaction/race or runtime-role proof is claimed.

## Synthetic browser observations

An isolated loopback server prohibited every database connection and stubbed owner
queries/commands. Real templates, forms, CSS, pending-input script and CSRF remained
active. The fixture has a bounded lease and synthetic identities only.

- Ordinary inventory/detail, explicit approval/decline and author cancellation
  reached simulated retained outcomes. These are not real independent decisions.
- Empty, expired, historical, denied, dependency-failure and overflow states were
  observed; denied/failure states exposed no recipient or private rationale.
- A conflict preserved exact original input and retry key, focused the alert and
  did not resubmit automatically.
- Navigation from pending input remained on the detail with input intact, but the
  browser did not expose its native dialog. No genuine Cancel/discard result is
  claimed; actual native controls remain #92 work.
- At 320, 390, 768, 958, 1024, 1280 and 1920 CSS pixels there was no horizontal
  page overflow, with one H1/main and labelled controls. Visual review found a small
  submit target; dedicated scoped CSS corrected the select and submit to 44 pixels
  at every width. The cached fixture template was restarted before confirming the
  actual new stylesheet and dimensions.
- Keyboard Tab/Space/Enter kept visible focus and completed simulated approval.
  No browser warning/error was returned during the final ordinary path.

Actual 200% zoom, reduced-motion acceptance, representative screen-reader/two-human
comprehension and the joined native journey remain #92/#109 evidence, not inferred
from component rendering. Exact-commit certification and protected delivery remain
required after final feedback. #108 and #48 remain open.

## Pre-delivery review

Review of the actual source-selector contract found that the new reader initially
called a transaction-only source selector in preflight. Preflight now uses ordinary
policy; both persistent-source checks occur only after canonical locks inside the
transaction. A dedicated database-free regression asserts that ordering. The
focused stack then passed 185 cases in 1.77s, before one final overflow-guidance
case. This reinforces that mocked component success is not native acceptance.
The author-terminal synthetic fixture was also corrected not to redisplay a cancel
control after terminal state; a real rendered regression protects that state.

Final complete database-free feedback passed 9,509 tests in 69.65 seconds,
including 86 new reader/form/view cases. Strict mypy passed all 707 source files.
The final overflow message initially exceeded the view's lint complexity limit;
factoring its unavailable-response helper preserved guidance without exemptions.
Ruff passed. Browser tabs and the temporary viewport were restored after rehearsal.
Exact-commit certification and hosted protected acceptance remain separate gates.
