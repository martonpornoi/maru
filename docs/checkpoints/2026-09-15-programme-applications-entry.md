# Dormant Programme Applications edition entry

Date: 2026-09-15. Scope: the existing #108 guided Applications and coherent
navigation checklist beneath #48, following protected PR #139. This is not
profile activation or completion of #108/#48.

## Outcome and contract map

The [entry contract](../product/page-contracts/programme-applications-entry.md)
connects an exact edition context to readable current-Department tasks for calls,
review setup, named-reviewer management, own reviewing, moderation and decisions.
Requirements PRG-001/003/006, AUD-001/003, NFR-013 and the shared-shell UX
contracts apply; ADRs 0082/0085/0100 remain unchanged.

The owner query validates exact active person, edition and adopted target before
complete bounded identifier discovery. Each of six code-owned task purposes
requires its own capability and destination field ceiling before any Department
label. Setup-only and reviewer-management-only grants stay distinct. Other
Departments/tasks reveal no names, identifiers or counts. Closed policy shape,
current version/reason/obligations, owner coherence and whole-source checks fail
closed. Required read/empty/denial audit contains no label or hidden total.
The final server-only fingerprint includes complete membership, decisions, labels,
edition lifecycle and profile pair; it is never rendered as a client capability.

The read-only shared-shell view accepts no POST or query-chosen authority. It
groups exact owning links by label/code, exposes only current permitted policy
sources and retains destination-owned assignment/independence/content checks.
Empty and read-only guidance is explicit. Rendering is bounded and the complete
owner projection is compared again before disclosure. No new migration, grant,
domain writer, domain event, outbox action or current-profile route is introduced.
The existing containment test recognizes only this exact dormant template's
shared CSS markers and retains its negative marker and production-route checks.

## Verification and actual failures

Focused entry/Department regressions passed 147 tests in 1.82s. Strict mypy
passes both new source files, NumPy docstrings pass, semantic quality validates
692 sources, and documentation validation passes 560 Markdown files/four skills/
215 requirements before this checkpoint. Whole Ruff/format passed 1,284 files.

The first complete unit run was not accepted: it found the new template's missing
exact shared-asset marker registration and 263 temporary-directory setup errors.
A one-test reproduction confirmed Windows denied access to the existing
`pytest-of-TheMw` temporary root. Nothing was deleted or repermissioned. A new
task-owned workspace temporary directory resolved that environmental failure;
the complete rerun passed 8,238 tests in 63.74s, with three existing URLField
warnings. The final complete rerun after the browser correction and added profile
change regression passes 8,239 tests in 64.68s, with the same three warnings.
No PostgreSQL test was collected or run. Fresh clean exact-commit certification
and independent hosted PR gate/CodeQL remain required before protected delivery.

## Synthetic browser evidence

The loopback-only fixture runs real entry query/form-free HTTP/templates/assets
and the existing call workspace over synthetic owner references and guarded
policy seams. All database connections and writes are forbidden. It does not
prove native policy, persistence or representative human acceptance.

The manager saw only Manage calls and Set up review; an insufficient manager
context field did not expose Manage reviewers. Keyboard Enter on Manage calls
opened the actual Department call workspace and its labelled call inventory.
The reviewer saw only their separately admitted My assigned reviews in another
Department. Mixed roles displayed two same-name Departments distinguished by
code, with only the corresponding tasks. Empty guidance exposed no hidden count;
read-only planning retained inspection links and explicit lifecycle guidance.
Denied (404) and unavailable (503) states disclose no prepared Department labels.
The task-owned browser tab and both exact fixture server processes were stopped
after their respective rehearsals; no other tabs or services were changed.

The first browser pass found duplicate Access disclosures from the shared host.
The final template suppresses only the generic host disclosure and retains the
computed task-specific one. A regression asserts one H1, one main and one Access
summary. The fresh restarted fixture verified those counts and no horizontal
overflow at 1280px (client/scroll both 1280px). Tab from Home reached the Access
summary with `:focus-visible`. No native zoom, full-width, screen-reader,
reduced-motion or two-human acceptance is claimed.

## Retained debt and next scope

Three maintained native cases cover visible/empty current-owner isolation and
real audit rollback under explicitly isolated future-profile/policy admission.
They remain uncollected/unexecuted
[#102 debt](https://github.com/martonpornoi/maru/issues/102#issuecomment-5685179217),
with no invented costs, weights or combined coverage. Full restored real-policy,
coverage and timing proof remains mandatory.
[Human follow-up](https://github.com/martonpornoi/maru/issues/92#issuecomment-5685179628)
retains #92's actual-role, width/zoom and assistive-technology matrix.

Current profiles, production routing and dormant conversion continuations are
unchanged. Direct conversion-only entry requires additional Programme authority
and exact adapters; it is not inferred from this list. Other owner launchers,
safe-file intake/viewers, accountable setup and #109/#102/#97/#92 gates remain
unfinished. The fresh safe-file schema-only exception is still unanswered.
