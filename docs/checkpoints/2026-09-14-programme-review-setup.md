# Dormant Programme review-policy setup

Date: 2026-09-14. Branch: `codex/programme-review-setup`, from protected
PR #120 (`75e2f37b1e7f8cc5b42470373b68304c4440977c`). This is one #108
guided-journey increment inside #48, not completion or activation of either.

## Outcome and boundaries

Applications owns exact-Department review-setup call discovery, complete bounded
question configuration and single-version immutable policy history. These
audited queries independently request `review_setup` under the existing manager
capability, separate from case/assignment `review_context`. No proposal response,
person directory, score or private case discussion is exposed.

The ordinary form composer works on one explicit stage at a time. It supports
one to eight stages, independently chosen review counts, complete rubric rows,
labelled questions, explicit anonymity/discussion choices and all four deliberate
recipient templates/receipt rules. Optional criterion controls use native
disclosure, with populated or invalid rows kept open. Unsaved stages/templates
remain bounded closed request state, never new persisted models or authority.
Complete summaries prepare question labels once; they do not repeatedly scan
500 questions for every stage selection. No score threshold, ranking or outcome
is invented. Final reason and confirmation call the canonical append-only writer.

The guided composer requires call configuration frozen by activation or retained
after retirement. This does not remove the existing trusted kernel Draft-policy
operation, require the call to be open for submissions, or publish anything.
Old policy cases remain pinned. Conflicts retain original input/proof; an explicit
refresh retains the proposed policy while creating new optimistic/retry intent.
Same-intent saves still reach the canonical replay boundary when planning closes.

The optional exact `CASE_OPENED.reference_id` now guards the selected submitted
seal under existing writer locks. Canonical input fields and absent-reference
digests remain unchanged for historical receipts. Changed seals conflict, and
other actions retain their required reference semantics. The case chooser and
case-opening browser task remain the next #108 increment, not a dead link here.

Requirements: PRG-003/004/006/009, AUD-001, QRY-005 through QRY-008 and the
shared-shell UX contracts. ADR 0085 remains authoritative; ADR 0100 retains the
explicit PostgreSQL development deferral. No schema, migration, runtime privilege,
production route, active profile, dependency or retention-purpose change.
Existing immutable evidence/recovery rules apply; no deletion is introduced.

## Verification actually performed

- Complete database-free preflight: **6,733 passed in 41.35s**, with three existing
  URLField deprecation warnings. Earlier full preflight: 6,730 passed and one
  shared-shell partial assertion failed. The repair explicitly proves this exact
  partial is included by the existing shared-shell parent; it is not a broad
  template exemption. Focused scope/form/view/selected-seal/containment checks
  passed 123 before two additional recovery cases.
- The initial oversized-input test used an unbounded generated test ID and old
  Windows temporary storage; compact explicit IDs and fresh task-owned temp
  storage corrected the fixture errors. Existing files/ACLs were not changed.
- Ruff, focused mypy (five new modules), professional NumPy docstrings and
  maintained documentation validation pass. Clean-commit certification and
  independent hosted acceptance remain pending at this checkpoint.
- Synthetic in-app browser journey: call discovery; labelled stage controls;
  explicit rubric/question/anonymity/discussion; four recipient templates and
  receipt choices; full review; reasoned confirmed synthetic save; immutable
  history; read-only context; generic denied detail. One H1/main and no document
  overflow at 1280 CSS pixels (document width 1265) were observed. The history
  screenshot was inspected. A follow-up observed one open initial criterion and
  15 optional collapsed ones after the compact-control refinement.
- The fixture uses real views/forms/templates/assets but stubbed authority,
  queries and commands, with all database connections forbidden and fixture-only
  CSRF bypass. It proves no persistence. Real CSRF/method guards, malformed scope,
  stale/late-render denial, reorder/edit/remove, explicit intent refresh and
  populated-error expansion are automated HTTP evidence, not human acceptance.
  Both task-owned servers and tabs were stopped/closed.
- Native setup-query acceptance and selected-seal conflict/exact retry cases
  are maintained without collection or execution. They remain #102 debt.

## Next bounded work

Certify this exact clean candidate, obtain green protected hosted acceptance,
merge and synchronize main, then deliver the independently authorized labelled
case chooser/opening and named reviewer management. Continue reviewer conflict/
score/discussion, moderation, final decisions and accepted conversion. Readable
retained recipient source context, authorized reference/file choices, wider
timetable/staffing/notice/continuity connections and setup also remain #108 work.
Record genuine responsive, keyboard, zoom, screen-reader and manager-comprehension
acceptance in #92; #102/#97/#109/#92 precede final promotion.
