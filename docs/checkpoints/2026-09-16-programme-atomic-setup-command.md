# Dormant atomic Programme setup command (#108)

Requirements: IDN-011/012, EVT-001/002/006/007, HR-009, UX-020/030, NFR-013;
ADR 0081 and the accepted Programme Operations setup contract. This increments
accountable setup, not its independently approved operational authority, genuine-
person continuation, guided surface, full fixture or final profile promotion.

Events now composes public owner commands within one transaction. The exact
supported profile and active platform principal are checked before database work;
current Identity is rechecked before owner work and locked before final evidence.
Actor/key serialization admits complete retained replay only. New requests require
selectable exact v1 and setup integrity, join canonical shared authority fences,
lock/recheck reused foundation fingerprints, retain truthful representation, and
create one new edition and first Department. Child replay without a complete setup
receipt conflicts. Owner evidence/effects, setup audit and immutable receipt commit
together; failures propagate through rollback. Opaque results imply no invitation,
approval, operational grant or activation. Both current profiles and runtime
SELECT-only boundaries are unchanged; no new migration or mounted route exists.

Complete database-free feedback passed **9,197 tests in 67.16 seconds**, with three
existing URL-field deprecation warnings. Focused orchestration/reference/schema
feedback passed 123 tests. Strict types passed 696 source files; Ruff, NumPy and
semantic Python documentation passed (717 source files). Initial documentation
lint exposed an exception-factory inference mismatch; public raises were made
explicit, without changing error behaviour or weakening the validator.

Nine additional native scenarios are maintained in the existing setup schema file:
three create/reuse modes with retained exact retry/conflict, three final-write
rollback points checking owner/audit/effect counts, real actor revocation on replay,
changed source after preview and zero-query current-profile denial. They remain
**uncollected and unexecuted**, bringing that file to 36 declared cases. The existing
transaction-local candidate manifest is limited foundation scaffolding, not the
complete Programme profile or integrated/runtime proof. Concurrency/race acceptance,
logical restore, runtime grants and the genuine end-to-end fixture remain final
#102/#97/#109 debt; mocked rollback tests do not prove native rollback.

The earlier explicitly approved setup schema-only observation is not repeated or
represented as command acceptance. No database test suite, profile activation,
Docker resource, production data or runtime grant was used in this increment.
Clean exact-commit certification and protected hosted delivery remain pending.
