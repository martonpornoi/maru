# Dormant Programme operator run sheets

Date: 2026-09-13\
Issue: [#100](https://github.com/martonpornoi/maru/issues/100), native child of #48\
Boundary: Local implementation and synthetic acceptance; protected delivery pending

## Outcome and source contract

ADR 0099 adds independently authorized room, exact Department and edition run
sheets. Department membership follows current Venue room responsibility plus
deliberately adopted Workforce binding lineage, including retained predecessors;
it never infers Programme item ownership from an Applications proposal.

Scheduling verifies the active canonical/native release. Programme independently
supplies exact selected reviewed copy and only requested current technical,
accessibility or media instructions. Venues supplies current wayfinding. Requested
Workforce detail supplies current demand instructions separately from retained
interval/state counts, without a person roster. No read changes or relocates work,
and confirmation is not proof of attendance or renewed eligibility.

One bounded typed query drives private JSON, transparent context calendars and
shared-Administration-shell HTML/print. Every requested layer independently
authorizes, including on an empty result. Missing, denied, oversized or moving
evidence cannot become a complete partial run sheet. Withdrawn/invalidated
releases withhold approved rows and calendars; saved files cannot be recalled.

Authorization 0030 adds five edition-or-narrower capability codes without grants
or profile activation. Real unused reversal/reapply and retained revoked-grant
or role-bundle downgrade fences are covered. Required recorder/function readiness
changes and fix-forward instructions accompany the migration. Historical tests
are explicitly classified; unmeasured new groups retain the conservative maximum
fallback, not a fabricated timing calibration.

## Verification and repairs

- The complete 17-case focused PostgreSQL run passed in 299.59 seconds, including
  156.36 seconds of fresh schema setup. It covers exact scopes, foreign person/
  tenant denial, native withdrawal, audit failure, requested-column minimization,
  a Department with work but no room, retained predecessors, real HTTP reads and
  native migration reversal/fences. The longest individual call was 12.83 seconds
  for construction and verification of the complete staffing lineage.
- The final fast unit suite passed 4,986 tests in 34.20 seconds. Two existing
  Django URL-field deprecation warnings remain. An additional 168-case targeted
  repair run passed, covering composition, nonce policy and acceptance inventory.
- Repository-wide Ruff/formatting, strict mypy, NumPy docstrings and semantic
  documentation validation passed. A fresh warning-fatal Sphinx build passed;
  exact-commit certification will rebuild final documentation and rerun all gates.
- Earlier fixture failures were corrected, not waived: the staffing setup lacked
  an ordinary Shift-management grant; historic catalogs needed explicit exclusion
  of the later additive codes; a Windows wrapper initially treated a favicon
  warning on native stderr as process failure. Temporary-directory permission
  errors were resolved with an isolated test base directory.

## Synthetic browser evidence

The opt-in `tests/rehearsals/programme_operator.py` uses loopback-only disposable
PostgreSQL, synthetic accounts, real owner commands/read policy and test-only
dormant admission. Its full-profile staffing setup is not Programme-only
activation or evidence of zero excluded-module setup records.

Observed authorized absent/populated room, Department and edition pages; a
Department with no room but linked work; default exclusion of optional fields;
explicit all-layer output; distinct cancelled/removed predecessor and confirmed
current work; room-only denial of edition/private-layer requests; ungranted-person
denial; source-unavailable and native withdrawn states; sign-out authentication;
print-friendly source preservation; and actual JSON/calendar download events.
Browser-controlled downloads are not calendar-client or native-print acceptance.

The strict page CSP initially blocked the shared inline navigation controller.
A fresh per-response nonce now admits only that controller, without
`unsafe-inline`. The browser then exposed existing shared-shell dialog/focus
gaps: collapsed details links were included in the focus loop and native summaries
were excluded. The repair preserves the desktop navigation landmark, gives the
open mobile drawer a labelled modal-dialog role, includes summaries and excludes
closed details content. Fresh-browser checks verified Close, Escape, reverse-Tab
wrap to the last visible Account summary, Tab wrap to Close, hidden/inert
background and focus return to the opener.

Final synthetic widths 320, 390, 768, 958, 1,024, 1,280 and 1,920 CSS pixels had
one H1/main and no page-level horizontal overflow. At 390 pixels keyboard focus
had a visible solid outline and stayed in view; Space selection and Enter
submission produced the requested technical-only result. No browser warning/error
was reported in the inspected successful-page log sample. A three-second download
observer initially timed out; a handled longer bounded observer verified both
downloads. Fixture leases include interactive waiting and are not test-runtime
benchmarks. Completed fixture containers were removed by exact task-owned ID;
intermediate teardown reported a still-connected session before container removal.

## Remaining delivery and activation gates

This checkpoint is not an exact-commit certificate, protected merge, runtime
deployment or production approval. Final local certification must retain every
required PostgreSQL group, combined branch-aware coverage and measured timing
headroom; the current exact head must then pass GitHub's unchanged PR gate and
CodeQL before squash merge and exact-main synchronization.

Native print, genuine 200% zoom, screen-reader/representative comprehension and
calendar-client/private-file handling remain explicitly unpassed under
[#92](https://github.com/martonpornoi/maru/issues/92#issuecomment-5652149284).
No profile, production route, runtime writer or general sending is activated.
Detailed change delivery, on-site continuity, #97 logical recovery, guided setup
and integrated Programme-only acceptance remain subsequent #48 work.

### Additional pre-commit HTTP verification

The two existing real HTTP cases were extended to withdraw the exact reviewed
copy before withdrawing the release. Both HTML and calendar correctly withheld
invalidated copy and earlier content. That focused rerun passed two cases with
12 deselected in 183.31 seconds, including 159.94 seconds of fresh schema setup;
its exact disposable container was removed. It adds state coverage without
adding another heavyweight fixture case. Full exact-commit acceptance is still
required.
