# Dormant Programme item-workspace increment

Date: 2026-09-14
Scope: First reviewable #108 increment under #48; PR preparation, not activation.
Base: protected PR #111, `fa155a143945b4d7533d0b4f8fa6f9cbd826bfcf`.

## Delivered local behavior

The [page contract](../product/page-contracts/programme-item-workspace.md)
defines labelled complete item selection and organizer-created core work without
invented proposals, hosts or reviews. Separate working, delivery, readiness
applicability and public-copy pages call the existing owner commands. Selected
history requires its own exact field ceiling and truthfully shows at most 200
entries. Private copy is never automatically inserted into public approval.
Current profiles, production URL configuration and runtime writers stay unchanged.

New owner queries provide the exact creation control and displayed working-source
reference under canonical parent/edition locks, initial/final policy and required
audit. They neither broaden existing generic projection types nor import another
module's private state. Strict forms preserve original version, source and retry
identity after refusal, recheck authority before returning private input, and use
CSRF plus no-store responses. Native before-unload protection warns for edited or
returned pending forms without autosave, browser persistence or new activity data.

No migrations, schema-only execution, privilege changes, production keys, personal
data or profile activation. Rollback removes only dormant adapters; existing
commands and append-only evidence retain their established recovery contract.

## Focused verification

- 86 database-free form, HTTP and query-boundary tests passed in 1.46 seconds.
  Real forms/templates and owner-query composition run against separately
  substituted policy/storage boundaries. Tests cover disclosure ordering,
  independent fields, unknown/duplicate input, CSRF, preserved stale/retry tokens,
  reauthorization, mandatory audit failure, complete inventory bounds, escaping,
  retained-history ceilings and dormant production routing.
- Five frontend tests passed for the actual pending-input asset: unchanged/CSRF
  state, editing, retained failure, deliberate submit/page restoration and detached
  or read-only pages. Test body time 69ms; process duration 17.28s includes setup.
- Strict mypy passed all four workspace source files; NumPy documentation passed.
  Focused type feedback corrected Django field-order typing; documentation feedback
  corrected propagated-loader error descriptions. Ruff and diff whitespace passed.
- The first HTTP test run caught overly broad test selectors that included the
  shared shell's logout form and an incorrect assumption that Django's admin
  catch-all would not resolve. Assertions now check this workspace's forms and
  actual absence of its production view, without changing runtime routing.
- Two native cases are maintained in `tests/integration/test_programme_queries.py`
  but **not executed** under ADR 0100. They cover coherent source/version inputs
  through real commands, current-profile denial, wrong-edition isolation and
  required-audit failure. Debt is recorded in #102. No native timing or combined
  coverage claim is made.

Exact-commit retained development certification and independent hosted acceptance
remain required after this checkpoint. Focused evidence is not a substitute.

## Synthetic browser observations

The disposable loopback fixture refuses all database connections. Owner policy,
queries and commands are substituted, using synthetic exact IDs and no real
people. The first fixture lacked the shared admin reverse namespace; this was
corrected in the fixture only. No production URL or authorization bypass was added.

At 1280 by 720, ordinary inventory/item selection, working/delivery/readiness/
public-copy continuations, empty and view-only inventory, and a returned stale
working form were inspected. Observed one H1 and one main landmark, no page-level
horizontal overflow, explicit private/public separation, blank public-entry
fields, retained edited title/reason and focused error alert after stale refusal.
Final forms lead with the work before its rationale and use readable readiness
labels. Visual inspection identified inherited low-contrast public-copy heading
styling; the workspace explicitly uses the body foreground for these headings.

These are component observations, not real authority, successful database writes,
the complete width matrix, keyboard traversal, genuine zoom, native discard
interaction, screen-reader or accessibility certification. Those checks remain
#92 work. HTTP denied/unavailable behavior is automated evidence, not a claimed
full browser failure-state matrix. No user's animation preference was changed.

## Issue reconciliation and next work

#48's close event was linked to PR #111's squash at 2026-09-13 21:34:48 UTC,
although its checklist still had unfinished outcomes. PR prose included a
negated closing-keyword phrase. On live verification it was replaced with ordinary
parent references and the umbrella reopened. #108/#109/#102/#97/#92 were verified
open. Do not use negated closing-keyword phrases for unfinished parent outcomes.

#108 now records reviewable increments: finish typed evidence/discussion/
withdrawal/hosting, Applications calls/proposals/review/conversion, timetable/
staffing/release/notice/continuity task connections, accountable setup and the
isolated fixture. Final promotion still depends on #102/#97/#92/#109. This
increment does not complete #108 or the #48 delivery decomposition.
