# Programme rehearsal composition preparation

Date: 2026-09-18
Scope: #108's isolated complete-fixture prerequisite beneath #48; preparation only.

## Outcome and boundaries

Added a closed test-only `programme_operations@1` candidate, an independent
internal-fact Effects handler registry and one joined rehearsal URLconf. The
manifest pins literal owner capabilities, shell kinds, catalogs, adapters,
conflicts and events. It preserves Workforce/foundation behavior without adopting
Registration, Participation, payment, attendance, Logistics or general
Communications. All 27 Programme role recipes are declared without assigning any
role, replacing its scope, or supplying an approval.

The independent registry reuses the real internal acknowledgement handler;
production dormant-event exclusions remain unchanged. All ten dormant owner URL
modules compose with the existing shell. Every named owner route round-trips to
its original handler, converted scope and defaults without shadowing; current
application and baseline URLconfs still do not mount those handlers.

Imports select no runtime URLconf, change no current manifest or model choices,
register no selectable/persisted profile and open no database. Owner-catalog
validation deliberately continues to report the candidate's persistence-key
mismatch: no schema installation or promotion is claimed. No migration, authority,
current setting, runtime ACL or production route changed.

## Verification and limits

Focused database-free candidate/routing checks: **448 passed in 1.50s**.
The pre-existing focused candidate/dormancy run passed 78 cases in 0.97s.
Complete database-free feedback passed **10,285 cases in 59.47s**, with three
existing Django URLField deprecation warnings. Ruff's two constant-comparison
style findings in the new routing test were corrected without behavioral change.
These tests inspect real owner catalogs and route resolution; they do not execute
the joined pages or prove native commands, worker delivery, genuine-person
decisions or recovery. No PostgreSQL case was collected or executed and no fixture
server or database was started. Fresh exact-commit development certification and
hosted protected delivery must be recorded separately when obtained.

This increment does not finish the complete isolated fixture. The next work is
the policy-fenced disposable database/runtime boundary, exact candidate schema
installation and truthful command-driven foundation/role scenarios, followed by
the P01–P12 assertions. Existing component authorizer substitutions and
transaction-local schema fixtures are not an implementation shortcut or evidence
for that boundary. #102's restored native acceptance, #97's logical recovery,
#92's actual human work and #109's joined proof remain mandatory. #108 and #48
remain open; both production profiles remain unchanged.
